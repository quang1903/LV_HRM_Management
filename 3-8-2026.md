# Nhật ký công việc & Báo cáo Tối ưu hóa Kỹ thuật (03/08/2026)

---

## 📋 I. TỔNG QUAN CÁC HẠNG MỤC ĐÃ THỰC HIỆN TRONG NGÀY

1. **Xác minh & Phân tích Chức năng Thêm Nhân viên (Excel & Nhập tay)**.
2. **Phân tích Kiến trúc & Tự động hóa Phân quyền Mô-đun Phòng ban (`departments`)**.
3. **Chuẩn hóa Định dạng Ngày/Tháng/Năm (`DD/MM/YYYY`) Toàn diện trên 5 Màn hình Frontend**.
4. **Khắc phục Lỗi Re-render Biểu đồ "Xoay 3-4 lần" trên Trang Báo cáo (`reports-panel.tsx`)**.
5. **Tối ưu Luồng Xử lý & Sửa Lỗi TypeScript cho Máy Quét QR Code (`app/scan/page.tsx`)**.

---

## 🔍 II. CHI TIẾT KẾT QUẢ PHÂN TÍCH & GIẢI PHÁP KỸ THUẬT

### 1. Cơ chế Tạo Nhân viên & Tự động Tạo Tài khoản (`employee.controller.js`)
* **Thủ công & Import Excel**: Cả 2 phương thức đều lưu dữ liệu nhân viên vào bảng `employees`.
* **Khởi tạo Tài khoản tự động**:
  * Tự động thêm bản ghi vào bảng `users` với `employee_id` liên kết.
  * `username`: Tự động cắt từ phần trước `@` của Email (xử lý trùng lặp tự động thêm đuôi số `nguyenvana1`, `nguyenvana2`...).
  * `password`: Khởi tạo mật khẩu mặc định là `123456` (đã mã hóa bcrypt).
  * `is_active`: `= 1` (Kích hoạt sẵn để nhân viên có thể đăng nhập ngay).

---

### 2. Mô-đun Phòng ban & Tự động hóa Phân quyền Trưởng phòng
* **Liên kết 2 chiều**: Giữa `departments.manager_id` và `employees.position_id`.
* **Cơ chế ghi đè Trưởng phòng**: Mỗi phòng ban chỉ lưu duy nhất 1 Trưởng phòng. Khi bổ nhiệm Trưởng phòng mới (B):
  * **B** được gán làm Trưởng phòng mới, tài khoản tự động nâng role lên `manager`.
  * **A** (Trưởng phòng cũ) tự động bị gỡ vị trí Trưởng phòng, tài khoản bị hạ role từ `manager` về `employee` (nếu không quản lý phòng nào khác).
* **Phân tích UX Chức vụ**: Phân tích hiện tượng ẩn/hiện Chức vụ do lọc cứng `department_id = ?`, đề xuất giải pháp Chức vụ dùng chung (`department_id IS NULL`) để tối ưu trải nghiệm người dùng.

---

### 3. Chuẩn hóa Định dạng Ngày `DD/MM/YYYY` trên Frontend

#### a. Bổ sung Hàm tiện ích dùng chung (`lib/utils.ts`)
```typescript
/**
 * Chuyển đổi chuỗi ngày từ YYYY-MM-DD (hoặc ISO) sang định dạng Việt Nam DD/MM/YYYY
 * Ví dụ: "2026-08-02" -> "02/08/2026"
 */
export function formatDate(dateStr?: string | null): string {
  if (!dateStr) return "—"
  const cleanDateStr = dateStr.substring(0, 10)
  const [y, m, d] = cleanDateStr.split("-")
  if (!y || !m || !d || y.length !== 4) return dateStr
  return `${d}/${m}/${y}`
}
```

#### b. Áp dụng cho 5 Màn hình chính:
1. **`attendance-table.tsx`**: Định dạng ngày `work_date` trên Bảng chấm công và Modal sửa chấm công.
2. **`employee-table.tsx`**: Định dạng `hire_date` (Ngày vào) và `birth_date` (Ngày sinh) trong Modal xem chi tiết nhân viên.
3. **`leave-table.tsx`**: Định dạng `start_date` và `end_date` trên Bảng và Modal xem chi tiết đơn nghỉ phép.
4. **`contract-table.tsx`**: Định dạng `start_date` và `end_date` trên Bảng hợp đồng (*giữ nguyên `substring(0, 10)` cho `<input type="date">` để không làm hỏng ô chọn ngày HTML5*).
5. **`reports-panel.tsx`**: Định dạng `end_date` ở bảng Hợp đồng sắp hết hạn.
6. **`activity-panel.tsx`**: Định dạng ngày nghỉ phép và hết hạn hợp đồng trên Dashboard.

---

### 4. Tối ưu Hiệu năng Biểu đồ & Xử lý Re-render (`reports-panel.tsx`)

#### a. Nguyên nhân Biểu đồ bị "Xoay 3-4 lần":
* Đối tượng `stats` được tạo mới (new object reference) ở mỗi lượt render của `ReportsPanel`.
* Do 4 API (`fetchAttendance`, `fetchDepartment`, `fetchLeave`, `fetchContract`) trả về ở 4 mốc thời gian khác nhau $\rightarrow$ `ReportsPanel` render 4 lần $\rightarrow$ `useEffect([stats])` trong `AttendanceDoughnutChart` chạy lại 4 lần $\rightarrow$ Biểu đồ bị `destroy()` và vẽ lại từ 0% liên tục.

#### b. Giải pháp bọc `useMemo`:
```typescript
import { useState, useEffect, useRef, useMemo } from "react"

const stats = useMemo(() => ({
  dung_gio: attendanceData.reduce((sum, e) => sum + Number(e.on_time || 0), 0),
  di_tre: attendanceData.reduce((sum, e) => sum + Number(e.late || 0), 0),
  vang_mat: attendanceData.reduce((sum, e) => sum + Number(e.absent || 0), 0),
  ve_som: attendanceData.reduce((sum, e) => sum + Number(e.early_leave || 0), 0),
}), [attendanceData])
```
* **Kết quả**: `stats` giữ nguyên vùng nhớ tham chiếu và chỉ tính toán lại khi `attendanceData` thực sự thay đổi. Biểu đồ chỉ vẽ và chạy animation đúng **1 lần duy nhất**!

---

### 5. Tối ưu & Sửa Lỗi Máy quét QR Chấm công (`app/scan/page.tsx`)

#### a. Tối ưu Luồng Xử lý Quét Mã (`onScanSuccess`):
* Gọi `scannerRef.current?.pause(true)` ngay đầu hàm khi phát hiện mã QR để ngăn camera gửi trùng đúp request do độ trễ mạng.
* Bọc khối `try ... finally` để đảm bảo `scannerRef.current?.resume()` luôn được gọi tự động sau 2 giây cho lượt quét tiếp theo.

#### b. Sửa 3 Lỗi TypeScript Compile-time (Problems: 0):
* Thêm `as any` cho thuộc tính cấu hình camera `experimentalFeatures`.
* Thêm `as const` cho `type: "in" as const` và `type: "out" as const` trong `setHistory`.

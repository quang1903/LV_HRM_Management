# Ghi chú Công việc ngày 29/06/2026

## 1. Những gì ĐÃ LÀM ĐƯỢC (Đã fix trong Code)
- **Vá lỗi Logic Chấm Công (Lỗi #1):** Đã sửa thành công lỗi "nối giờ làm qua ngày hôm sau" trong hai hàm `checkOut` và `selfCheckOut` (`backend/src/controllers/attendance.controller.js`). Bổ sung điều kiện `work_date = today` để ngăn lấy nhầm bản ghi treo từ hôm trước.
- **Vá lỗ hổng Bypass Số ngày nghỉ phép (Lỗi #2):** Backend không còn tin tưởng thông số `total_days` từ Client gửi lên trong `createLeave` (`backend/src/controllers/leave.controller.js`). Hệ thống đã tự động tính lại số ngày nghỉ bằng hàm `countWorkingDays` (loại bỏ Chủ nhật). Đã đồng bộ logic tính toán này lên cả file giao diện Frontend (`frontend/components/hrm/leave-table.tsx`).
- **Tối ưu Hiệu năng Import File Excel (Lỗi #3):** Đã đưa hàm mã hóa mật khẩu `bcrypt.hash("123456", 10)` ra bên ngoài vòng lặp trong hàm `importEmployees` (`backend/src/controllers/employee.controller.js`). Từ giờ, server sẽ chỉ tính mã hóa đúng 1 lần thay vì hàng nghìn lần, khắc phục hoàn toàn tình trạng giật lag, lãng phí CPU khi import dữ liệu lớn.
- **Bảo đảm Toàn vẹn Dữ liệu chuyển Trưởng phòng (Lỗi #4):** Đã áp dụng `Transaction` (bắt đầu bằng `conn.beginTransaction()` và kết thúc bằng `conn.commit()`) vào hàm `updateDepartment` (`backend/src/controllers/department.controller.js`). Việc cập nhật `manager_id` và nâng/hạ quyền (`role`) người dùng giờ đã được gói gọn trong một chu kỳ duy nhất. Nếu sập nguồn/mất kết nối giữa chừng, `conn.rollback()` sẽ đưa dữ liệu về trạng thái ban đầu, loại bỏ hoàn toàn tình trạng "bóng ma quyền lực" (lệch dữ liệu giữa 2 bảng).

## 2. Những gì CHƯA LÀM ĐƯỢC (Chờ xử lý / Đợi duyệt)
- **Chưa có chức năng tự động dọn rác/fix bản ghi treo:** Nếu nhân viên quên checkout hôm trước, bản ghi sẽ vĩnh viễn có `work_minutes = 0` và `check_out = NULL`. 

## 3. Kế hoạch Test / Hướng dẫn Test thủ công (Do chưa chạy test)

### Dành cho Lỗi #1 (Tính sai giờ làm / Quên Checkout)
**Trường hợp: Mô phỏng "Quên checkout ngày hôm trước"**
- **Bước 1:** Vào `PHPMyAdmin`, mở bảng `attendances`. Tìm 1 bản ghi chấm công của ngày hôm trước, cố tình **sửa tay cột `check_out` = NULL** (để giả lập).
- **Bước 2:** Đăng nhập đúng vào tài khoản nhân viên đó trên Web.
- **Bước 3:** **Không** bấm Check-in mới cho hôm nay. Bấm ngay nút **Check-out**.
- **Kết quả kỳ vọng:** Hệ thống phải báo lỗi: **"Bạn chưa check-in hôm nay, không thể check-out"**. Xác nhận lỗi nối nhầm giờ cũ đã bị triệt tiêu.

### Dành cho Lỗi #2 (Gian lận số ngày nghỉ phép)
**Chi tiết Lỗi:** Trước đây Frontend tự đếm ngày và gửi số lên, Backend lưu thẳng số đó. Nếu một nhân viên dùng Postman gửi `start_date` là mùng 1, `end_date` là mùng 30 nhưng cố tình truyền `total_days: 1` thì Backend vẫn cho phép họ nghỉ cả tháng mà chỉ trừ 1 ngày phép. Việc tính ngày nghỉ cũng bị sai luật nếu tính gộp luôn cả ngày Chủ nhật.

**Các bước test chi tiết:**
- **Bước 1 (Test thuận trên UI):** Mở trình duyệt, vào trang Gửi đơn nghỉ phép. Chọn Từ ngày: **Thứ 6 (31/07/2026)**, Đến ngày: **Thứ 4 tuần sau (05/08/2026)**.
- **Kỳ vọng Bước 1:** Dòng chữ hiển thị trên form phải là **5 ngày** (vì thực tế là 6 ngày nhưng đã tự động loại đi 1 ngày Chủ nhật 02/08).
- **Bước 2 (Test ngược chống Hacker - Rất quan trọng):** Mở F12 -> Console trên trình duyệt (để lợi dụng có sẵn token đăng nhập) và chạy đoạn code ép buộc gửi dữ liệu sai:
```javascript
fetch("http://localhost:5000/api/leaves", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer " + localStorage.getItem("hrm_access_token")
  },
  body: JSON.stringify({
    request_type: "Nghi phep",
    start_date: "2026-08-01",
    end_date: "2026-08-31",
    total_days: 1,   // cố tình gửi số gian lận
    reason: "test gian lan"
  })
}).then(r => r.json()).then(d => console.log(d))
```
- **Kỳ vọng Bước 2:** Mở PHPMyAdmin -> bảng `leave_requests`, tìm đúng đơn vừa tạo bằng đoạn code trên. Cột `total_days` trong Database KHÔNG THỂ LÀ 1. Nó phải tự động cập nhật thành số ngày thật tế (khoảng 26-27 ngày). Điều này chứng minh Backend đã hoàn toàn "miễn nhiễm" với các hành vi gian lận dữ liệu.

### Dành cho Lỗi #3 (Hiệu năng Import Excel)
- **Bước 1:** Chuẩn bị 1 file Excel mẫu với khoảng 100 - 500 nhân viên (dùng tính năng Export để lấy file mẫu rồi copy ra cho nhiều dòng).
- **Bước 2:** Tiến hành Import file đó trên hệ thống. Nếu được, mở F12 -> Network để đo thời gian Request.
- **Kỳ vọng Bước 2:** Quá trình Import phải diễn ra "cực kỳ nhanh" (có thể chỉ dưới 1-2 giây) thay vì bắt người dùng phải chờ đợi lâu như trước đây. Server không có dấu hiệu bị "đơ".
- **Bước 3 (Quan trọng nhất - Xác nhận chức năng KHÔNG bị hỏng):** Đăng nhập bằng vài tài khoản nhân viên (không chỉ dòng đầu tiên) từ danh sách vừa import, sử dụng mật khẩu mặc định `123456`.
- **Kỳ vọng Bước 3:** Tất cả đều phải đăng nhập thành công. Điều này chứng minh việc dùng chung `defaultPasswordHash` hoạt động hoàn hảo cho mọi tài khoản.

### Dành cho Lỗi #4 (Mất đồng bộ chuyển Trưởng phòng)
**Nhóm 1 — Test chức năng vẫn hoạt động đúng như trước**
- **Bước 1:** Đăng nhập Admin → vào trang Phòng ban.
- **Bước 2:** Sửa 1 phòng, đổi Trưởng phòng từ người A sang người B (B đang là employee).
- **Kỳ vọng:** Thông báo hiện đúng: "Cập nhật phòng ban thành công. Đã tự động nâng quyền tài khoản Trưởng phòng mới lên Quản lý. Đã tự động hạ quyền tài khoản Trưởng phòng cũ về Nhân viên...". Vào trang Người dùng kiểm tra thấy B thành Quản lý, A thành Nhân viên.

**Nhóm 2 — Test xác nhận Transaction hoạt động (Rollback khi gặp sự cố)**
- **Bước 1:** Tạm thời mở file `department.controller.js` và chèn dòng `throw new Error("TEST: Giả lập lỗi giữa Transaction")` ngay bên dưới lệnh cập nhật role `manager` cho Trưởng phòng mới.
- **Bước 2:** Khởi động lại Backend và thử đổi Trưởng phòng.
- **Kỳ vọng Bước 2:** Nhận được thông báo "Lỗi server".
- **Bước 3:** Vào lại Database hoặc trang Người dùng kiểm tra.
- **Kỳ vọng Bước 3:** Dù đã có lệnh nâng quyền chạy phía trước, người mới vẫn chưa lên chức, người cũ vẫn là trưởng phòng. Dữ liệu không hề bị đổi do cơ chế `conn.rollback()` đã hoàn tác toàn bộ thay đổi.
- *(Đừng quên xóa dòng ném lỗi tạm thời đó sau khi test xong!)*

# Ghi chú Công việc ngày 29/06/2026

## 1. Những gì ĐÃ LÀM ĐƯỢC (Đã fix trong Code)
- **Vá lỗi Logic Chấm Công (Lỗi #1):** Đã sửa thành công lỗi "nối giờ làm qua ngày hôm sau" trong hai hàm `checkOut` và `selfCheckOut` (`backend/src/controllers/attendance.controller.js`).
- Cụ thể: Bổ sung thêm điều kiện `work_date = today` vào câu truy vấn SQL tìm bản ghi checkout. Hệ thống giờ đây chỉ cho phép checkout bản ghi checkin của ngày hôm nay, ngăn chặn hoàn toàn việc "treo" giờ làm nếu nhân viên quên checkout ngày hôm trước và vô tình bấm checkout vào ngày hôm sau.

## 2. Những gì CHƯA LÀM ĐƯỢC (Chờ xử lý / Đợi duyệt)
- **Lỗ hổng Bypass Số ngày nghỉ phép (Lỗi #2):** Backend vẫn đang tin tưởng tuyệt đối `total_days` từ Frontend gửi lên trong `createLeave`.
- **Lỗi Hiệu Năng khi Import Excel (Lỗi #3):** Thuật toán `bcrypt.hash` vẫn đang bị đặt sai chỗ (nằm bên trong vòng lặp duyệt từng dòng Excel), có nguy cơ treo server khi import hàng loạt.
- **Lỗi Mất đồng bộ chuyển Trưởng phòng (Lỗi #4):** Các truy vấn cập nhật Role ở bảng `users` và `departments` vẫn chạy độc lập, thiếu khối bảo vệ `Transaction`.
- **Chưa có chức năng tự động dọn rác/fix bản ghi treo:** Nếu nhân viên quên checkout hôm trước, bản ghi sẽ vĩnh viễn có `work_minutes = 0` và `check_out = NULL`. 

## 3. Kế hoạch Test / Hướng dẫn Test thủ công (Do chưa chạy test)

Dưới đây là 2 kịch bản cần kiểm tra trực tiếp trên trình duyệt / hệ thống:

**Trường hợp 1: Test luồng bình thường**
- **Bước 1:** Đăng nhập tài khoản nhân viên.
- **Bước 2:** Bấm `Check-in` trong ngày hôm nay. (Ghi nhận giờ vào).
- **Bước 3:** Đợi khoảng 1-2 phút, bấm `Check-out` trong cùng ngày hôm nay.
- **Bước 4:** Xác nhận hệ thống vẫn hoạt động bình thường, thông báo "Check-out thành công" và cập nhật đúng số phút làm việc (`work_minutes`).

**Trường hợp 2: Test tình huống lỗi cũ (Mô phỏng "Quên checkout ngày hôm trước")**
- **Bước 1:** Vào `PHPMyAdmin`, mở bảng `attendances`.
- **Bước 2:** Tìm 1 bản ghi chấm công của ngày hôm trước (hoặc sửa ngày của một bản ghi cũ), cố tình **sửa tay cột `check_out` = NULL** (để giả lập việc nhân viên quên checkout).
- **Bước 3:** Đăng nhập đúng vào tài khoản nhân viên đó trên Web.
- **Bước 4:** **Không** bấm Check-in mới cho hôm nay. Bấm ngay nút **Check-out**.
- **Kết quả kỳ vọng:** Hệ thống phải chặn và báo lỗi rõ ràng: **"Bạn chưa check-in hôm nay, không thể check-out"**. Nếu hiện lỗi này, tức là lỗi #1 không còn khả năng tự động lấy nhầm bản ghi cũ để nối giờ.

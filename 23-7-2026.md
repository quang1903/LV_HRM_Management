# Nhật ký công việc ngày 23/7/2026 (Tổng hợp toàn bộ Lỗi & Nguyên nhân sửa)

Tài liệu này lưu trữ chi tiết toàn bộ **18 lỗi logic, bảo mật và tính năng** đã được rà soát, phát hiện và khắc phục hoàn chỉnh trong hệ thống Backend HRM ngày 23/7/2026.

---

## 🏆 CHI TIẾT CÁC LỖI ĐÃ SỬA VÀ NGUYÊN NHÂN SỬA

### 1. Vá lỗ hổng bảo mật IDOR xem Đơn nghỉ phép chi tiết
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L42-L57) — `getLeaveById`
* **Vấn đề & Nguyên nhân:** Lỗ hổng bảo mật IDOR. Hàm `getLeaveById` trước đây hoàn toàn không kiểm tra phân quyền người dùng, cho phép bất kỳ ai nhập ID đơn nghỉ phép trên API để xem nội dung nghỉ phép và lý do riêng tư của nhân sự khác.
* **Giải pháp đã áp dụng:** Thêm biến kiểm tra chính chủ `isSelf = rows[0].employee_id === req.user.employee_id`. Trả về lỗi `403 Forbidden` nếu nhân viên thường xem đơn của người khác hoặc Trưởng phòng xem đơn ngoài phạm vi quản lý.

---

### 2. Vá lỗ hổng bảo mật IDOR xem bản ghi Chấm công chi tiết
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L50-L65) — `getAttendanceById`
* **Vấn đề & Nguyên nhân:** Tương tự `getLeaveById`, API `GET /api/attendances/:id` trả về thông tin chi tiết lượt chấm công mà không kiểm tra quyền sở hữu hay quyền quản lý.
* **Giải pháp đã áp dụng:** Bổ sung logic kiểm tra `isSelf` và phân quyền chặn truy cập bất hợp pháp cho `employee` và `manager`.

---

### 3. Phân quyền danh sách Chấm công cho Trưởng phòng
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L26-L29) — `getAttendances`
* **Vấn đề & Nguyên nhân:** Hàm `getAttendances` chỉ có bộ lọc cho `employee`, thiếu bộ lọc theo phòng ban cho `manager`, dẫn tới việc Trưởng phòng xem được chấm công của toàn bộ nhân viên trong công ty.
* **Giải pháp đã áp dụng:** Bổ sung Prepared Statement `AND e.department_id IN (SELECT id FROM departments WHERE manager_id = ?)` khi `req.user.role === "manager"`.

---

### 4. Phân quyền Báo cáo Phòng ban cho Trưởng phòng
* **File liên quan:** [report.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/report.controller.js#L42-L56) — `getDepartmentReport`
* **Vấn đề & Nguyên nhân:** API `/api/reports/department` lấy tất cả dữ liệu phòng ban mà không lọc theo Manager đang đăng nhập.
* **Giải pháp đã áp dụng:** Thêm mệnh đề `WHERE d.id IN (SELECT id FROM departments WHERE manager_id = ?)` khi người dùng có vai trò `manager`.

---

### 5. Chặn tước quyền Admin/HR khi phân công Trưởng phòng
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L167-L172) — `updateEmployee`
* **Vấn đề & Nguyên nhân:** Khi đổi chức vụ nhân viên sang "Trưởng phòng...", code cũ ép cứng `UPDATE users SET role = 'manager'`. Nếu tài khoản đó đang là `admin` hoặc `hr`, họ sẽ bị tước mất quyền quản trị.
* **Giải pháp đã áp dụng:** Kiểm tra `role !== 'admin' && role !== 'hr'` trước khi thực thi nâng role lên `manager`.

---

### 6. Tự động nâng quyền Manager khi Tạo phòng ban mới
* **File liên quan:** [department.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/department.controller.js#L43-L50) — `createDepartment`
* **Vấn đề & Nguyên nhân:** `createDepartment` khi chọn `manager_id` chỉ lưu vào bảng `departments` mà quên nâng `users.role` của nhân viên đó lên `manager`.
* **Giải pháp đã áp dụng:** Tự động nâng `users.role = 'manager'` cho Trưởng phòng mới ngay khi phòng ban được khởi tạo thành công.

---

### 7. Định dạng chuẩn DATETIME khi Admin sửa Chấm công
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L110-L125) — `updateAttendance`
* **Vấn đề & Nguyên nhân:** Khi Admin sửa giờ check-in/out, frontend gửi chuỗi giờ `HH:MM`. Code cũ lưu trực tiếp làm MySQL bị lỗi cú pháp DATETIME hoặc lưu sai ngày.
* **Giải pháp đã áp dụng:** Lấy `work_date` gốc của bản ghi và áp dụng `formatTime(check_in, work_date)` để đưa về chuỗi chuẩn `YYYY-MM-DD HH:MM:00`.

---

### 8. Chuẩn hóa Múi giờ UTC độc lập môi trường Server khi Đếm ngày phép & Duyệt đơn
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L102-L175) — `countWorkingDays` & `approveLeave`
* **Vấn đề & Nguyên nhân:** `new Date("YYYY-MM-DD")` dùng UTC ISO String, khi gọi `.getDay()` trên server có timezone khác UTC+7 (như Server Cloud AWS/Vercel) sẽ đếm trượt Thứ 7 / Chủ nhật.
* **Giải pháp đã áp dụng:** Tách chuỗi ngày và chuyển sang dùng `Date.UTC(sy, sm - 1, sd)` kết hợp `.getUTCDay()` và `.setUTCDate()`.

---

### 9. Hủy manager_id & hạ role Trưởng phòng khi Nhân viên nghỉ việc
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L205-L212) — `deactivateEmployee`
* **Vấn đề & Nguyên nhân:** Khi vô hiệu hóa nhân viên (`status = 'Nghi viec'`), hệ thống quên tước `manager_id` của phòng ban do họ từng quản lý.
* **Giải pháp đã áp dụng:** Bổ sung `UPDATE departments SET manager_id = NULL WHERE manager_id = ?` và hạ role về `employee`.

---

### 10. Dọn dẹp bản ghi phụ thuộc tránh lỗi Foreign Key khi Xóa vĩnh viễn Nhân viên
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L229-L236) — `permanentDelete`
* **Vấn đề & Nguyên nhân:** Lệnh `DELETE FROM employees` bị văng lỗi HTTP 500 do bị các bảng `attendances`, `leave_requests`, `contracts`, `profile_change_requests` chặn khóa ngoại.
* **Giải pháp đã áp dụng:** Thực thi xóa dọn dẹp tất cả dữ liệu ở các bảng phụ thuộc trước khi xóa bản ghi `employees`.

---

### 11. Chặn Gia hạn Hợp đồng cho Nhân viên đã nghỉ việc
* **File liên quan:** [contract.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/contract.controller.js#L85-L93) — `renewContract`
* **Vấn đề & Nguyên nhân:** `renewContract` không kiểm tra `status` của nhân viên, cho phép gia hạn hợp đồng cho người đã nghỉ việc.
* **Giải pháp đã áp dụng:** Bổ sung truy vấn kiểm tra `empRows[0].status === 'Nghi viec'`, trả về lỗi 400 không cho phép gia hạn.

---

### 12. Tự động đồng bộ Role Trưởng phòng khi Đổi tên Chức vụ
* **File liên quan:** [position.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/position.controller.js#L74-L103) — `updatePosition`
* **Vấn đề & Nguyên nhân:** Khi Admin sửa tên một chức vụ (vd đổi từ "Chuyên viên" thành "Trưởng phòng..."), hệ thống chỉ update tên chức vụ mà không nâng role cho nhân sự đang giữ chức vụ đó.
* **Giải pháp đã áp dụng:** Quét danh sách nhân viên đang giữ chức vụ đó và đồng bộ `users.role` cũng như `departments.manager_id` tương ứng.

---

### 13. Chặn Duyệt yêu cầu thay đổi thông tin cá nhân của Nhân viên đã nghỉ việc
* **File liên quan:** [profileRequest.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/profileRequest.controller.js#L90-L96) — `approveRequest`
* **Vấn đề & Nguyên nhân:** `approveRequest` cho phép duyệt đổi SĐT/Địa chỉ cho nhân sự đã thôi việc.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra `empRows[0].status === 'Nghi viec'` trước khi cho phép duyệt.

---

### 14. Tự động chuyển trạng thái Về sớm (Ve som) khi quét QR Check-out
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L260-L266) — `checkOut`
* **Vấn đề & Nguyên nhân:** Hàm `checkOut` giữ nguyên `status` từ lúc check-in ngay cả khi nhân viên ra về sớm trước 17:00 chiều.
* **Giải pháp đã áp dụng:** Kiểm tra giờ check-out theo múi giờ Việt Nam, nếu `hour < 17` và trạng thái là `Dung gio`, tự động chuyển thành `Ve som`.

---

### 15. Dọn dẹp Chức vụ liên kết tránh lỗi Foreign Key khi Xóa Phòng ban
* **File liên quan:** [department.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/department.controller.js#L151-L154) — `deleteDepartment`
* **Vấn đề & Nguyên nhân:** Lệnh `DELETE FROM departments` bị văng lỗi HTTP 500 nếu phòng ban đó còn các Chức vụ (`positions`) liên kết.
* **Giải pháp đã áp dụng:** Thực thi `DELETE FROM positions WHERE department_id = ?` trước khi xóa phòng ban.

---

### 16. Chặn Tạo tài khoản User cho Nhân viên đã nghỉ việc
* **File liên quan:** [user.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/user.controller.js#L64-L70) — `createUser`
* **Vấn đề & Nguyên nhân:** `createUser` cho phép gán tài khoản đăng nhập mới cho nhân sự đã nghỉ việc.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra `empRows[0].status === 'Nghi viec'` trước khi tạo user.

---

### 17. Chặn Tạo hợp đồng mới cho Nhân viên đã nghỉ việc
* **File liên quan:** [contract.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/contract.controller.js#L61-L64) — `createContract`
* **Vấn đề & Nguyên nhân:** `createContract` không kiểm tra `status` của nhân viên khi tạo mới hợp đồng.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra `emp[0].status === 'Nghi viec'`, trả về lỗi 400 chặn tạo hợp đồng.

---

### 18. Tự động cấp role Manager khi Tạo mới Nhân viên gán chức Trưởng phòng
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L127-L146) — `createEmployee`
* **Vấn đề & Nguyên nhân:** `createEmployee` luôn gán mặc định `role = 'employee'` cho tài khoản user mới ngay cả khi nhân viên đó được chọn chức vụ "Trưởng phòng...".
* **Giải pháp đã áp dụng:** Kiểm tra `position_id`, nếu chứa chữ "Trưởng phòng...", tự động nâng `users.role = 'manager'` và gán `departments.manager_id`.

---

### 19. Chặn Trưởng phòng tự duyệt đơn nghỉ phép của chính mình
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L131-L135) — `approveLeave`
* **Vấn đề & Nguyên nhân:** Trưởng phòng nộp đơn xin nghỉ phép cho chính mình thì câu truy vấn kiểm tra quyền quản lý phòng ban vẫn khớp, cho phép họ tự bấm duyệt đơn của chính mình.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra `if (existing[0].employee_id === req.user.employee_id)`, trả về lỗi HTTP `403 Forbidden` buộc đơn của Trưởng phòng phải do Admin/HR duyệt.

---

### 20. Dọn dẹp bản ghi Chấm công Vắng mặt khi Từ chối Đơn nghỉ phép
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L217-L228) — `rejectLeave`
* **Vấn đề & Nguyên nhân:** Đơn sau khi duyệt đã tạo các ngày `Vang mat` trong bảng `attendances`. Nếu sau đó đơn bị từ chối (`rejectLeave`), backend không dọn dẹp các bản ghi này làm nhân viên bị tính vắng mặt sai.
* **Giải pháp đã áp dụng:** Bổ sung câu lệnh `DELETE FROM attendances WHERE employee_id = ? AND work_date BETWEEN ? AND ? AND status = 'Vang mat' AND check_in IS NULL` khi bấm từ chối đơn.

---

### 21. Chặn tạo trùng lặp khoảng ngày Nghỉ phép
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L83-L91) — `createLeave`
* **Vấn đề & Nguyên nhân:** `createLeave` thiếu bước kiểm tra trùng lặp thời gian, cho phép nhân viên nộp nhiều đơn xin nghỉ đè trùng lên cùng một khoảng ngày.
* **Giải pháp đã áp dụng:** Bổ sung truy vấn `overlap` kiểm tra `status != 'Tu choi'` và `start_date <= ? AND end_date >= ?` trước khi cho phép tạo đơn mới.

---

### 22. Chặn Quét mã QR Check-in ghi đè lên ngày đã duyệt Nghỉ phép
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L196-L203) — `checkIn`
* **Vấn đề & Nguyên nhân:** Ngày được duyệt nghỉ phép đã có bản ghi `Vang mat`. Nếu nhân viên cố tình/vô tình quét QR check-in, backend sẽ tìm thấy bản ghi và ghi đè thành `Dung gio`/`Di tre`.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra bản ghi ngày hôm đó nếu có `status = 'Vang mat' AND check_in IS NULL` thì trả về lỗi 400 không cho phép check-in.

---

### 23. Xóa triệt để Cookie `hrm_user` khi Token hết hạn / Lỗi 401 ở Frontend
* **File liên quan:** [api.ts](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/services/api.ts#L64-L68) — Interceptor
* **Vấn đề & Nguyên nhân:** Khi Refresh Token hết hạn, `api.ts` xóa `localStorage` nhưng quên xóa Cookie `hrm_user`, làm Next.js Middleware vẫn tưởng user đã login, gây lỗi treo trang hoặc vòng lặp redirect.
* **Giải pháp đã áp dụng:** Bổ sung dòng `document.cookie = "hrm_user=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;"` xóa sạch cookie trước khi chuyển hướng về `/login`.

---

### 24. Chuẩn hóa Múi giờ Việt Nam cho Tháng/Năm mặc định trong Báo cáo
* **File liên quan:** [report.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/report.controller.js#L8-L10) — `getAttendanceReport` & `getLeaveReport`
* **Vấn đề & Nguyên nhân:** `new Date().getMonth() + 1` trên server UTC làm báo cáo mặc định bị lùi sang tháng cũ vào đầu tháng (00:00 - 07:00 AM ICT).
* **Giải pháp đã áp dụng:** Chuẩn hóa khởi tạo tháng/năm mặc định bằng múi giờ Việt Nam `Asia/Ho_Chi_Minh`.

---

### 25. Reset `position_id` của Nhân viên tránh Lỗi Khóa ngoại (Foreign Key) khi Xóa Phòng ban
* **File liên quan:** [department.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/department.controller.js#L152-L155) — `deleteDepartment`
* **Vấn đề & Nguyên nhân:** Lệnh `DELETE FROM positions` bị văng lỗi 500 nếu có nhân viên (đang thuộc phòng ban khác) vẫn tham chiếu tới `position_id` thuộc phòng bị xóa.
* **Giải pháp đã áp dụng:** Bổ sung `UPDATE employees SET position_id = NULL WHERE position_id IN (SELECT id FROM positions WHERE department_id = ?)` trước khi xóa chức vụ.

---

### 26. Chặn Gia hạn Hợp đồng đã Chấm dứt & Chặn trùng Hợp đồng đang Hiệu lực
* **File liên quan:** [contract.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/contract.controller.js#L96-L105) — `renewContract`
* **Vấn đề & Nguyên nhân:** `renewContract` cho phép gia hạn lại hợp đồng cũ đã bị chấm dứt (`Da cham dut`), làm một nhân viên có 2 hợp đồng cùng đang có hiệu lực.
* **Giải pháp đã áp dụng:** Chặn gia hạn nếu hợp đồng cũ có `status === 'Da cham dut'` hoặc nhân viên đã có hợp đồng hiệu lực khác.

---

### 27. Hủy `manager_id` Phòng ban khi Đổi tên Chức vụ khỏi Trưởng phòng
* **File liên quan:** [position.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/position.controller.js#L103) — `updatePosition`
* **Vấn đề & Nguyên nhân:** Khi đổi tên chức vụ từ "Trưởng phòng..." sang tên thường, `users.role` bị hạ về `employee` nhưng `departments.manager_id` vẫn giữ nguyên ID người cũ.
* **Giải pháp đã áp dụng:** Bổ sung `UPDATE departments SET manager_id = NULL WHERE manager_id = ?` khi chức vụ bị đổi tên khỏi Trưởng phòng.

---

### 28. Thêm bước Kiểm tra Tồn tại Tài khoản trước khi Đặt lại Mật khẩu & Reset Thiết bị
* **File liên quan:** [user.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/user.controller.js#L147-L157) — `resetPassword` & `resetDevice`
* **Vấn đề & Nguyên nhân:** Gọi API với User ID không tồn tại nhưng câu lệnh UPDATE không kiểm tra affectedRows, vẫn trả về HTTP 200 báo thành công.
* **Giải pháp đã áp dụng:** Kiểm tra `result.affectedRows === 0` ngay sau lệnh `UPDATE`, trả về HTTP 404 nếu không tìm thấy tài khoản (gọn gàng, nguyên tử và chỉ cần 1 truy vấn SQL).

---

### 29. Chặn Giờ Check-out nhỏ hơn Giờ Check-in khi Admin Chỉnh sửa Chấm công
* **File liên quan:** [attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L145) — `updateAttendance`
* **Vấn đề & Nguyên nhân:** Khi Admin sửa giờ check-out nhỏ hơn check-in, backend tính `work_minutes < 0` và tự động gán về 0 mà không cảnh báo lỗi cho Admin.
* **Giải pháp đã áp dụng:** Bổ sung kiểm tra `if (check_in && check_out && checkOutFormatted <= checkInFormatted)`, trả về lỗi HTTP 400 yêu cầu nhập giờ ra lớn hơn giờ vào.

### 30. Ẩn nút Duyệt / Từ chối đối với Đơn nghỉ phép của chính mình ở Frontend
* **File liên quan:** [leave-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/leave-table.tsx#L276) — Bảng và Modal Xem chi tiết Đơn nghỉ phép
* **Vấn đề & Nguyên nhân:** Trưởng phòng khi xem danh sách đơn vẫn nhìn thấy 2 nút "Duyệt" và "Từ chối" trên dòng đơn do chính mình gửi.
* **Giải pháp đã áp dụng:** Bổ sung điều kiện `leave.employee_id !== user?.employee_id` ở cả bảng và modal chi tiết, ẩn hoàn toàn nút Duyệt/Từ chối trên đơn của bản thân, chỉ cho phép HR/Admin hoặc Trưởng phòng khác duyệt.

---

## 💡 TỔNG KẾT GIÁ TRỊ NGIỆP VỤ & BẢO MẬT HỆ THỐNG
1. **Bảo mật dữ liệu (Anti-IDOR & Self-Approval):** Triệt tiêu 100% nguy cơ rò rỉ dữ liệu và ngăn chặn Trưởng phòng tự duyệt đơn của chính mình.
2. **Chuẩn hóa Phân cấp Quản trị:** Đảm bảo Trưởng phòng chỉ thấy và duyệt dữ liệu trong phạm vi phòng ban được phân công, HR/Admin xử lý các đơn của cấp Trưởng phòng.
3. **An toàn tài khoản & Đồng bộ Role:** Bảo vệ vai trò Admin/HR và tự động dọn dẹp Cookie/Storage nhất quán ở cả Frontend và Backend.
4. **Hệ thống chạy mượt & Không sập DB:** Xử lý dứt điểm các lỗi ranh giới (Foreign Key Constraint, định dạng DATETIME, Múi giờ UTC Server, đè ngày nghỉ phép, gia hạn hợp đồng, validate giờ chấm công).

---

## 📊 BẢN TÓM TẮT THIẾT KẾ PHÂN QUYỀN 7 KHỐI NGHIỆP VỤ HỆ THỐNG (RBAC ARCHITECTURE)

### 1. Quản lý phòng ban
* **`Admin`**: Thêm, Sửa, Xóa phòng ban và gán Trưởng phòng.
* **`HR / Quản lý / Nhân viên`**: Xem thông tin danh mục phòng ban.

### 2. Quản lý chức vụ
* **`Admin`**: Thêm, Sửa, Xóa chức vụ.
* **`HR / Quản lý / Nhân viên`**: Xem thông tin danh mục chức vụ.

### 3. Quản lý nhân viên
* **`Admin / HR`**: Thêm mới, Chỉnh sửa, Khóa hồ sơ, Import Excel, Duyệt đề xuất sửa SĐT/Địa chỉ.
* **`Quản lý / Nhân viên`**: Xem thông tin hồ sơ nhân viên (Quản lý xem nhân viên phòng mình, Nhân viên xem hồ sơ cá nhân).

### 4. Quản lý hợp đồng
* **`Admin / HR`**: Tạo mới, Gia hạn, Chấm dứt hợp đồng và Xem cảnh báo hết hạn 30 ngày.
* **`Quản lý / Nhân viên`**: Xem thông tin danh sách Hợp đồng lao động (Quản lý xem hợp đồng nhân viên phòng mình, Nhân viên xem hợp đồng cá nhân).

### 5. Quản lý chấm công
* **`Quản lý / Nhân viên`**: Quét mã QR TOTP 30s + định vị GPS Check-in/out cá nhân và xem lịch sử công.
* **`Admin / HR`**: Xem và Điều chỉnh giờ chấm công thủ công toàn công ty (Admin cấu hình vị trí GPS & Máy quét).

### 6. Quản lý đơn từ (Nghỉ phép)
* **`Quản lý / Nhân viên`**: Tạo và Gửi đơn xin nghỉ phép cá nhân (tự đếm ngày trừ Chủ nhật).
* **`Quản lý`**: Phê duyệt hoặc từ chối đơn của nhân viên thuộc phòng ban mình phụ trách (không được tự duyệt đơn của chính mình).
* **`HR`**: Phê duyệt đơn xin nghỉ phép của Trưởng phòng hoặc các trường hợp đặc biệt; tự động dọn dẹp/cập nhật hồ sơ vắng mặt.
* **`Admin`**: Không tham gia duyệt nghiệp vụ đơn từ hàng ngày, chỉ quản trị hệ thống.

### 7. Báo cáo & thống kê
* **`Admin / HR / Quản lý`**: Xem báo cáo thống kê và Trích xuất file Excel (`.xlsx`) (Admin/HR xem/xuất toàn công ty; Quản lý xem/xuất phòng ban mình).

---

## ❓ GIẢI ĐÁP CÁC CÂU HỎI NGHIỆP VỤ & PHÂN QUYỀN CHẤM CÔNG

### Q1: Người có quyền chỉnh sửa bảng chấm công ở Mục 5 là ai?
* **Giải đáp:** Trong mã nguồn hệ thống (`attendance.routes.js`), cả **Admin** và **HR** đều có quyền xem và chỉnh sửa/thêm giờ chấm công thủ công toàn công ty. **Quản lý (Trưởng phòng)** chỉ có quyền xem bảng công nhân viên thuộc phòng ban mình. **Nhân viên** tự thực hiện quét mã QR TOTP 30s + GPS để check-in/out cá nhân.

### Q2: Trưởng phòng Kế toán có được gọi là "người chấm công nhân viên" không?
* **Giải đáp:** Không. Trưởng phòng Kế toán (vai trò `Manager`) chỉ thực hiện **theo dõi bảng công phòng ban** đối với nhân viên thuộc phòng Kế toán. **Phòng Nhân sự (`HR`)** mới là đơn vị duy nhất chịu trách nhiệm **quản lý & điều chỉnh bảng chấm công toàn công ty** để phục vụ chốt công tính lương.

---

## 📐 QUY TẮC THIẾT KẾ SƠ ĐỒ TUẦN TỰ (SEQUENCE DIAGRAM STANDARDS)

### 1. Nguyên tắc ngầm định khi tạo Sơ đồ Tuần tự:
- **Dùng ngôn ngữ nghiệp vụ tiếng Việt chuẩn học thuật:** Tuyệt đối không ghi tên API thô (`POST /api/...`) hay câu lệnh SQL thô trên các mũi tên tương tác để tránh bị thầy cô hỏi xoáy vào cú pháp kỹ thuật.
- **Tối ưu luồng vẽ phẳng (dùng `opt` thay vì `alt` lồng nhau):** Sử dụng các khối `opt` (Option/If đơn) để kiểm tra các điều kiện bắt lỗi tuần tự (Mã QR hết hạn, GPS ngoài phạm vi, trùng đơn nghỉ phép, đã check-in). Cách vẽ này giúp sơ đồ cực kỳ thoáng, phẳng, không bị vẽ các ô khung hình chữ nhật lồng nhau rườm rà trên Draw.io.
- **Định dạng đầu ra:** Chỉ xuất 1 mã Text Mermaid sạch nhất để người dùng Copy-Paste trực tiếp vào Draw.io (`Arrange -> Insert -> Advanced -> Mermaid`).

### 2. Mẫu Mã Mermaid Sơ đồ Tuần tự Check-in Chấm công (Chuẩn mẫu):

```text
sequenceDiagram
    autonumber
    actor User as Nhân viên / Quản lý
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend
    participant DB as Cơ sở dữ liệu (Database)

    User->>UI: 1. Quét mã QR TOTP + Bật định vị GPS
    UI->>Backend: 2. Gửi thông tin check-in (Mã QR & Tọa độ GPS)

    Backend->>Backend: 3. Kiểm tra Mã QR TOTP xoay 30 giây
    opt Nếu Mã QR hết hạn hoặc không hợp lệ
        Backend-->>UI: Báo lỗi "Mã QR không hợp lệ hoặc đã hết hạn"
        UI-->>User: Hiển thị lỗi Mã QR
    end

    Backend->>DB: 4. Lấy vị trí GPS & Bán kính cấu hình công ty
    DB-->>Backend: Trả về vị trí công ty
    Backend->>Backend: 5. Tính khoảng cách vị trí thực tế với công ty
    opt Nếu nằm ngoài bán kính cho phép của công ty
        Backend-->>UI: Báo lỗi "Bạn đang ở ngoài phạm vi công ty"
        UI-->>User: Hiển thị lỗi vị trí GPS
    end

    Backend->>DB: 6. Kiểm tra lịch nghỉ phép ngày hôm nay
    DB-->>Backend: Trả về thông tin đơn nghỉ phép
    opt Nếu đã được duyệt nghỉ phép hôm nay
        Backend-->>UI: Báo lỗi "Hôm nay bạn đã được duyệt nghỉ phép"
        UI-->>User: Chặn check-in & Báo lỗi
    end

    Backend->>DB: 7. Kiểm tra lượt chấm công ngày hôm nay
    DB-->>Backend: Trả về dữ liệu chấm công trong ngày
    opt Nếu đã check-in trước đó trong ngày
        Backend-->>UI: Báo lỗi "Hôm nay bạn đã check-in rồi"
        UI-->>User: Báo lỗi đã check-in
    end

    Backend->>Backend: 8. Xác định trạng thái (Đúng giờ / Đi trễ)
    Backend->>DB: 9. Lưu bản ghi chấm công mới vào Database
    DB-->>Backend: Xác nhận lưu thành công
    Backend-->>UI: 10. Trả về kết quả "Check-in thành công"
    UI-->>User: Hiển thị thông báo Check-in thành công
```

---

## 💻 TRỌN BỘ MÃ MERMAID CÁC SƠ ĐỒ TUẦN TỰ (SEQUENCE DIAGRAMS) DÙNG DÁN DRAW.IO

### 1. QUẢN LÝ PHÒNG BAN

#### Sơ đồ 1a: Thêm Phòng ban & Gán Trưởng phòng (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (department.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Nhập thông tin phòng ban mới (Tên phòng, Trưởng phòng)
    UI->>Backend: 2. Gửi yêu cầu thêm phòng ban mới

    Backend->>DB: 3. Kiểm tra tên phòng ban đã tồn tại chưa
    DB-->>Backend: Trả về kết quả kiểm tra
    opt Nếu tên phòng ban đã tồn tại
        Backend-->>UI: Báo lỗi "Tên phòng ban đã tồn tại"
        UI-->>Admin: Hiển thị lỗi trùng tên
    end

    Backend->>DB: 4. Lưu thông tin phòng ban mới vào Database
    DB-->>Backend: Trả về ID phòng ban vừa tạo

    alt Có gán Trưởng phòng (manager_id)
        Backend->>DB: 5. Tự động nâng vai trò users.role = 'manager' cho Trưởng phòng mới
        DB-->>Backend: Trả về xác nhận cập nhật role thành công
    else Không gán Trưởng phòng
        Backend->>Backend: 6. Bỏ qua bước nâng role
    end

    Backend-->>UI: 7. Trả về kết quả "Thêm phòng ban thành công"
    UI-->>Admin: Hiển thị thông báo Thêm thành công & Tải lại danh sách
```

#### Sơ đồ 1b: Chỉnh sửa Phòng ban & Đổi Trưởng phòng (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (department.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Nhập thông tin chỉnh sửa (Tên phòng, Trưởng phòng mới)
    UI->>Backend: 2. Gửi yêu cầu cập nhật phòng ban

    Backend->>DB: 3. Kiểm tra phòng ban có tồn tại không
    DB-->>Backend: Trả về thông tin phòng ban hiện tại
    opt Nếu không tìm thấy phòng ban
        Backend-->>UI: Báo lỗi "Không tìm thấy phòng ban"
        UI-->>Admin: Hiển thị lỗi
    end

    Backend->>DB: 4. Kiểm tra tên mới có bị trùng với phòng ban khác không
    DB-->>Backend: Trả về kết quả trùng tên
    opt Nếu tên mới bị trùng
        Backend-->>UI: Báo lỗi "Tên phòng ban đã bị trùng"
        UI-->>Admin: Hiển thị lỗi trùng tên
    end

    Backend->>DB: 5. Cập nhật thông tin phòng ban vào Database

    alt Có sự thay đổi Trưởng phòng (manager_id)
        Backend->>DB: 6. Đồng bộ lại role: Hạ role Trưởng phòng cũ (nếu hết quản lý) & Nâng role Trưởng phòng mới
        DB-->>Backend: Trả về xác nhận đồng bộ role thành công
    else Không thay đổi Trưởng phòng
        Backend->>Backend: 7. Bỏ qua bước đồng bộ role
    end

    Backend-->>UI: 8. Trả về kết quả "Cập nhật phòng ban thành công"
    UI-->>Admin: Hiển thị thông báo Cập nhật thành công
```

#### Sơ đồ 1c: Xóa Phòng ban (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (department.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Bấm nút Xóa phòng ban & Xác nhận xóa
    UI->>Backend: 2. Gửi yêu cầu xóa phòng ban

    Backend->>DB: 3. Kiểm tra phòng ban có tồn tại trong hệ thống không
    DB-->>Backend: Trả về thông tin phòng ban
    opt Nếu không tìm thấy phòng ban
        Backend-->>UI: Báo lỗi "Không tìm thấy phòng ban"
        UI-->>Admin: Hiển thị lỗi
    end

    Backend->>DB: 4. Reset position_id = NULL ở bảng employees cho các chức vụ thuộc phòng này
    DB-->>Backend: Xác nhận gỡ liên kết nhân viên thành công

    Backend->>DB: 5. Xóa các chức vụ (positions) liên kết với phòng ban bị xóa
    DB-->>Backend: Xác nhận xóa chức vụ thành công

    Backend->>DB: 6. Xóa bản ghi phòng ban (departments) ra khỏi Database
    DB-->>Backend: Xác nhận xóa phòng ban thành công

    Backend-->>UI: 7. Trả về kết quả "Xóa phòng ban thành công"
    UI-->>Admin: Hiển thị thông báo Xóa thành công & Tải lại danh sách
```

---

### 2. QUẢN LÝ CHỨC VỤ

#### Sơ đồ 2a: Thêm Chức vụ (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (position.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Nhập thông tin chức vụ mới (Tên chức vụ, Chọn Phòng ban)
    UI->>Backend: 2. Gửi yêu cầu thêm chức vụ mới

    Backend->>DB: 3. Kiểm tra tên chức vụ đã tồn tại trong phòng ban này chưa
    DB-->>Backend: Trả về kết quả kiểm tra
    opt Nếu tên chức vụ đã trùng trong phòng ban
        Backend-->>UI: Báo lỗi "Chức vụ đã tồn tại trong phòng ban này"
        UI-->>Admin: Hiển thị lỗi trùng tên
    end

    Backend->>DB: 4. Lưu thông tin chức vụ mới vào Database
    DB-->>Backend: Trả về ID chức vụ vừa tạo

    Backend-->>UI: 5. Trả về kết quả "Thêm chức vụ thành công"
    UI-->>Admin: Hiển thị thông báo Thêm thành công & Tải lại danh sách
```

#### Sơ đồ 2b: Chỉnh sửa Chức vụ & Đồng bộ Role (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (position.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Nhập thông tin chỉnh sửa (Tên chức vụ mới, Chọn phòng ban)
    UI->>Backend: 2. Gửi yêu cầu cập nhật chức vụ

    Backend->>DB: 3. Kiểm tra chức vụ có tồn tại không
    DB-->>Backend: Trả về thông tin chức vụ hiện tại
    opt Nếu không tìm thấy chức vụ
        Backend-->>UI: Báo lỗi "Không tìm thấy chức vụ"
        UI-->>Admin: Hiển thị lỗi
    end

    Backend->>DB: 4. Cập nhật thông tin chức vụ vào bảng positions

    alt Chức vụ bị đổi tên khỏi "Trưởng phòng" (Hạ quyền Trưởng phòng cũ)
        Backend->>DB: 5. Hạ role users.role = 'employee' & Reset manager_id = NULL cho phòng ban
        DB-->>Backend: Xác nhận đồng bộ hạ quyền thành công
    else Tên chức vụ giữ nguyên hoặc nâng lên Trưởng phòng
        Backend->>Backend: 6. Bỏ qua bước hạ quyền Trưởng phòng cũ
    end

    Backend-->>UI: 7. Trả về kết quả "Cập nhật chức vụ thành công"
    UI-->>Admin: Hiển thị thông báo Cập nhật thành công
```

#### Sơ đồ 2c: Xóa Chức vụ (Admin)
```text
sequenceDiagram
    autonumber
    actor Admin as Admin (Quản trị viên)
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (position.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    Admin->>UI: 1. Bấm nút Xóa chức vụ & Xác nhận xóa
    UI->>Backend: 2. Gửi yêu cầu xóa chức vụ

    Backend->>DB: 3. Kiểm tra có nhân viên nào đang giữ chức vụ này không
    DB-->>Backend: Trả về danh sách nhân viên đang giữ chức vụ
    opt Nếu chức vụ đang được gán cho nhân viên
        Backend-->>UI: Báo lỗi "Chức vụ đang được gán cho nhân viên, không thể xóa"
        UI-->>Admin: Hiển thị thông báo chặn xóa
    end

    Backend->>DB: 4. Xóa bản ghi chức vụ (positions) ra khỏi Database
    DB-->>Backend: Xác nhận xóa chức vụ thành công

    Backend-->>UI: 5. Trả về kết quả "Xóa chức vụ thành công"
    UI-->>Admin: Hiển thị thông báo Xóa thành công & Tải lại danh sách
```

---

### 3. QUẢN LÝ NHÂN VIÊN & IMPORT EXCEL

#### Sơ đồ 3a: Import Danh sách Nhân viên Hàng loạt từ File Excel (.xlsx) (Admin / HR)
```text
sequenceDiagram
    autonumber
    actor AdminHR as Admin / HR
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (employee.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    AdminHR->>UI: 1. Tải file Excel (.xlsx) danh sách nhân viên & Bấm Import
    UI->>Backend: 2. Gửi file Excel dữ liệu nhân viên

    Backend->>Backend: 3. Khởi tạo Transaction & Đọc cấu trúc bảng file Excel

    Backend->>DB: 4. Kiểm tra trùng lặp Mã nhân viên và Email trong Database
    DB-->>Backend: Trả về kết quả kiểm tra
    opt Nếu Mã nhân viên hoặc Email bị trùng lặp
        Backend->>Backend: Rollback Transaction (Hủy toàn bộ quá trình)
        Backend-->>UI: Báo lỗi "Mã nhân viên hoặc Email đã tồn tại"
        UI-->>AdminHR: Hiển thị thông báo lỗi Import
    end

    Backend->>Backend: 5. Mã hóa Mật khẩu mặc định (Bcrypt) cho từng tài khoản

    Backend->>DB: 6. Lưu danh sách hồ sơ nhân viên mới vào bảng employees
    DB-->>Backend: Trả về ID các nhân viên vừa tạo

    Backend->>DB: 7. Cấp tài khoản người dùng tương ứng vào bảng users

    alt Chức vụ gán thuộc nhóm "Trưởng phòng"
        Backend->>DB: 8. Nâng vai trò users.role = 'manager' & Cập nhật manager_id cho Phòng ban
        DB-->>Backend: Trả về xác nhận gán Trưởng phòng thành công
    else Chức vụ thông thường
        Backend->>Backend: 9. Bỏ qua bước gán Trưởng phòng
    end

    Backend->>DB: 10. Commit Transaction (Xác nhận lưu toàn bộ dữ liệu)
    DB-->>Backend: Trả về xác nhận thành công

    Backend-->>UI: 11. Kết quả "Import danh sách nhân viên thành công"
    UI-->>AdminHR: Hiển thị thông báo Import thành công & Tải lại danh sách
```

---

### 4. QUẢN LÝ CHẤM CÔNG THÔNG MINH

#### Sơ đồ 4a: Check-out Chấm công (Nhân viên / Quản lý)
```text
sequenceDiagram
    autonumber
    actor User as Nhân viên / Quản lý
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (attendance.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    User->>UI: 1. Quét mã QR TOTP + Bật định vị GPS
    UI->>Backend: 2. Gửi thông tin check-out (Mã QR & Tọa độ GPS)

    Backend->>Backend: 3. Kiểm tra Mã QR TOTP xoay 30 giây
    opt Nếu Mã QR hết hạn hoặc không hợp lệ
        Backend-->>UI: Báo lỗi "Mã QR không hợp lệ hoặc đã hết hạn"
        UI-->>User: Hiển thị lỗi Mã QR
    end

    Backend->>DB: 4. Lấy vị trí GPS & Bán kính cấu hình công ty
    DB-->>Backend: Trả về vị trí công ty
    Backend->>Backend: 5. Tính khoảng cách vị trí thực tế với công ty
    opt Nếu nằm ngoài bán kính cho phép của công ty
        Backend-->>UI: Báo lỗi "Bạn đang ở ngoài phạm vi công ty"
        UI-->>User: Hiển thị lỗi vị trí GPS
    end

    Backend->>DB: 6. Kiểm tra dữ liệu chấm công ngày hôm nay
    DB-->>Backend: Trả về thông tin chấm công trong ngày

    opt Nếu chưa thực hiện check-in ngày hôm nay
        Backend-->>UI: Báo lỗi "Bạn chưa check-in ngày hôm nay"
        UI-->>User: Chặn check-out & Báo lỗi chưa check-in
    end

    alt Nếu đã thực hiện check-out trước đó trong ngày
        Backend-->>UI: Báo lỗi "Hôm nay bạn đã check-out rồi"
        UI-->>User: Báo lỗi đã check-out
    else Chưa check-out trong ngày
        Backend->>Backend: 7. Tính tổng thời gian làm việc (Số phút) & Kiểm tra về sớm trước 17:00
        Backend->>DB: 8. Cập nhật giờ check-out & số phút làm việc vào Database
        DB-->>Backend: Xác nhận cập nhật thành công
        Backend-->>UI: 9. Trả về kết quả "Check-out thành công"
        UI-->>User: Hiển thị thông báo Check-out thành công
    end
```

#### Sơ đồ 4b: Điều chỉnh Bảng Chấm công Thủ công (Admin / HR)
```text
sequenceDiagram
    autonumber
    actor AdminHR as Admin / HR
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (attendance.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    AdminHR->>UI: 1. Nhập thông tin điều chỉnh chấm công (Giờ vào, Giờ ra, Trạng thái, Ghi chú)
    UI->>Backend: 2. Gửi yêu cầu cập nhật bản ghi chấm công

    Backend->>DB: 3. Kiểm tra bản ghi chấm công có tồn tại không
    DB-->>Backend: Trả về thông tin chấm công gốc (bao gồm work_date)
    opt Nếu không tìm thấy bản ghi chấm công
        Backend-->>UI: Báo lỗi "Không tìm thấy bản ghi chấm công"
        UI-->>AdminHR: Hiển thị lỗi
    end

    Backend->>Backend: 4. Đưa giờ vào và giờ ra về định dạng ngày giờ chuẩn (YYYY-MM-DD HH:MM:00)

    opt Nếu Giờ ra nhỏ hơn hoặc bằng Giờ vào (check_out <= check_in)
        Backend-->>UI: Báo lỗi "Giờ ra (check-out) phải lớn hơn giờ vào (check-in)"
        UI-->>AdminHR: Hiển thị thông báo lỗi giờ chấm công
    end

    Backend->>Backend: 5. Tính toán tổng số phút làm việc thực tế (work_minutes)

    Backend->>DB: 6. Cập nhật giờ vào, giờ ra, số phút làm việc, trạng thái và ghi chú vào Database
    DB-->>Backend: Xác nhận cập nhật thành công

    Backend-->>UI: 7. Trả về kết quả "Cập nhật chấm công thành công"
    UI-->>AdminHR: Hiển thị thông báo Cập nhật thành công & Tải lại danh sách
```

---

### 5. BÁO CÁO, THỐNG KÊ & TRÍCH XUẤT DỮ LIỆU

#### Sơ đồ 5a: Xem Báo cáo Thống kê & Trích xuất File Excel (.xlsx) (Admin / HR / Quản lý)
```text
sequenceDiagram
    autonumber
    actor User as Admin / HR / Quản lý
    participant UI as Giao diện Frontend
    participant Backend as Xử lý Backend (report.controller.js)
    participant DB as Cơ sở dữ liệu (Database)

    User->>UI: 1. Chọn Bộ lọc (Tháng, Năm, Phòng ban) & Bấm Xem báo cáo
    UI->>Backend: 2. Gửi yêu cầu lấy dữ liệu báo cáo thống kê

    Backend->>Backend: 3. Khởi tạo Tháng/Năm mặc định theo Múi giờ Việt Nam (Asia/Ho_Chi_Minh)

    alt Vai trò là Quản lý (Trưởng phòng)
        Backend->>DB: 4a. Truy vấn số liệu thống kê lọc theo phạm vi Phòng ban phụ trách
        DB-->>Backend: Trả về số liệu ngày công & nghỉ phép của phòng ban
    else Vai trò là Admin / HR
        Backend->>DB: 4b. Truy vấn số liệu thống kê toàn bộ các phòng ban trong Công ty
        DB-->>Backend: Trả về số liệu tổng hợp toàn công ty
    end

    Backend-->>UI: 5. Trả về dữ liệu báo cáo thống kê (JSON)
    UI-->>User: 6. Hiển thị bảng công, tỷ lệ nghỉ phép & biểu đồ thống kê

    User->>UI: 7. Bấm nút "Xuất file Excel"
    UI->>UI: 8. Tổng hợp dữ liệu & Trích xuất file Excel (.xlsx) tải về máy
    UI-->>User: 9. Tải xuống file Excel báo cáo thành công
```

---

## ⚡ TRỌN BỘ MÃ MERMAID CÁC SƠ ĐỒ HOẠT ĐỘNG (ACTIVITY DIAGRAMS) DÙNG DÁN DRAW.IO

### 1. QUẢN LÝ CƠ CẤU TỔ CHỨC (PHÒNG BAN & CHỨC VỤ)

#### Sơ đồ Hoạt động 1a: Thêm Phòng ban & Gán Trưởng phòng (Admin)
```text
flowchart TD
    Start([Bắt đầu Thêm Phòng ban]) --> Input[Admin Nhập Tên phòng ban & Chọn Trưởng phòng]
    Input --> CheckName{Tên phòng ban đã<br/>tồn tại trong DB?}
    
    CheckName -- Đã tồn tại --> ErrName[Báo lỗi: Tên phòng ban đã tồn tại] --> EndErr([Kết thúc - Lỗi])
    CheckName -- Chưa có --> InsertDept[(Lưu phòng ban mới vào bảng departments)]
    
    InsertDept --> CheckManager{Có chọn gán<br/>Trưởng phòng mới?}
    CheckManager -- Có chọn --> ElevateRole[Tự động nâng vai trò users.role = manager cho Trưởng phòng]
    CheckManager -- Không chọn --> KeepState[Bỏ qua bước nâng quyền]
    
    ElevateRole --> Success[Thông báo Thêm phòng ban thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepState --> Success
```

#### Sơ đồ Hoạt động 1b: Chỉnh sửa Phòng ban & Đổi Trưởng phòng (Admin)
```text
flowchart TD
    Start([Bắt đầu Sửa Phòng ban]) --> Input[Admin Nhập Tên mới & Chọn Trưởng phòng mới]
    Input --> CheckExist{Phòng ban có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy phòng ban] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> CheckDupName{Tên mới có bị trùng<br/>với phòng ban khác?}
    
    CheckDupName -- Trùng tên khác --> ErrDup[Báo lỗi: Tên phòng ban đã bị trùng] --> EndErr
    CheckDupName -- Không trùng --> UpdateDept[(Cập nhật Tên & Trưởng phòng mới vào DB)]
    
    UpdateDept --> CheckManagerChange{Có sự thay đổi<br/>Trưởng phòng?}
    CheckManagerChange -- Có đổi Trưởng phòng --> SyncManager[Hạ role Trưởng phòng cũ & Nâng users.role = manager cho Trưởng phòng mới]
    CheckManagerChange -- Không đổi --> KeepManager[Giữ nguyên Trưởng phòng hiện tại]
    
    SyncManager --> Success[Thông báo Cập nhật phòng ban thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepManager --> Success
```

#### Sơ đồ Hoạt động 1c: Xóa Phòng ban (Admin)
```text
flowchart TD
    Start([Bắt đầu Xóa Phòng ban]) --> ClickDelete[Admin Bấm nút Xóa phòng ban & Xác nhận]
    ClickDelete --> CheckExist{Phòng ban có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy phòng ban] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> CheckEmpDept{Phòng ban có đang<br/>chứa nhân viên không?}
    
    CheckEmpDept -- Đang có nhân viên --> BlockEmp[Báo lỗi: Phòng ban đang có nhân viên, không thể xóa] --> EndErr
    CheckEmpDept -- Không có nhân viên --> DeletePos[(Xóa các chức vụ thuộc phòng ban ở bảng positions)]
    
    DeletePos --> DeleteDept[(Xóa bản ghi Phòng ban ra khỏi bảng departments)]
    DeleteDept --> Success[Thông báo Xóa phòng ban thành công] --> EndSuccess([Kết thúc - Thành công])
```

#### Sơ đồ Hoạt động 1d: Thêm Chức vụ (Admin)
```text
flowchart TD
    Start([Bắt đầu Thêm Chức vụ]) --> Input[Admin Nhập Tên chức vụ & Chọn Phòng ban]
    Input --> CheckDupPos{Chức vụ đã tồn tại<br/>trong phòng ban này?}
    
    CheckDupPos -- Đã tồn tại --> ErrPos[Báo lỗi: Chức vụ đã tồn tại trong phòng ban này] --> EndErr([Kết thúc - Lỗi])
    CheckDupPos -- Chưa có --> InsertPos[(Lưu chức vụ mới vào bảng positions)]
    
    InsertPos --> Success[Thông báo Thêm chức vụ thành công] --> EndSuccess([Kết thúc - Thành công])
```

#### Sơ đồ Hoạt động 1e: Chỉnh sửa Chức vụ & Đồng bộ Role (Admin)
```text
flowchart TD
    Start([Bắt đầu Sửa Chức vụ]) --> Input[Admin Nhập Tên chức vụ mới & Chọn Phòng ban mới]
    Input --> CheckExist{Chức vụ có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy chức vụ] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> UpdatePos[(Cập nhật Tên & Phòng ban vào bảng positions)]
    
    UpdatePos --> CheckRenamed{Chức vụ bị đổi tên<br/>khoải Trưởng phòng?}
    CheckRenamed -- Có đổi khỏi Trưởng phòng --> DemoteRole[Hạ users.role = employee & Cập nhật manager_id = NULL cho phòng ban]
    CheckRenamed -- Không đổi tên Trưởng phòng --> KeepRole[Bỏ qua bước hạ quyền Trưởng phòng]
    
    DemoteRole --> Success[Thông báo Cập nhật chức vụ thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepRole --> Success
```

#### Sơ đồ Hoạt động 1f: Xóa Chức vụ (Admin)
```text
flowchart TD
    Start([Bắt đầu Xóa Chức vụ]) --> ClickDelete[Admin Bấm nút Xóa chức vụ & Xác nhận]
    ClickDelete --> CheckEmpPos{Có nhân viên nào<br/>đang giữ chức vụ này?}
    
    CheckEmpPos -- Đang có nhân viên giữ --> BlockDelete[Báo lỗi: Chức vụ đang được gán cho nhân viên, không thể xóa] --> EndErr([Kết thúc - Lỗi])
    CheckEmpPos -- Không có nhân viên giữ --> DeletePos[(Xóa bản ghi Chức vụ ra khỏi bảng positions)]
    
    DeletePos --> Success[Thông báo Xóa chức vụ thành công] --> EndSuccess([Kết thúc - Thành công])
```

---

### 2. QUẢN LÝ NHÂN VIÊN & IMPORT EXCEL

#### Sơ đồ Hoạt động 2a: Thêm Hồ sơ Nhân viên (Thủ công & Import Excel) (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Thêm Nhân viên]) --> Method{Chọn phương thức<br/>thực hiện?}
    
    Method -- 1. Thêm thủ công --> ManualInput[Admin/HR Nhập Hồ sơ Nhân viên]
    ManualInput --> CheckDupManual{Kiểm tra trùng Mã NV<br/>hoặc Email trong DB?}
    CheckDupManual -- Có trùng --> ErrManual[Báo lỗi: Mã NV hoặc Email đã tồn tại] --> EndErr([Kết thúc - Lỗi])
    CheckDupManual -- Không trùng --> HashPass[Mã hóa Mật khẩu mặc định bằng Bcrypt]
    HashPass --> InsertManual[(Lưu thông tin vào bảng employees & users)] --> CheckLeader
    
    Method -- 2. Import từ Excel --> UploadExcel[Admin/HR Tải file Excel .xlsx & Đọc dữ liệu]
    UploadExcel --> Transact[Khởi tạo DB Transaction]
    Transact --> CheckDupExcel{Kiểm tra trùng Mã NV<br/>hoặc Email trong DB?}
    CheckDupExcel -- Có trùng --> Rollback[Rollback Transaction: Hủy toàn bộ] --> ErrExcel[Báo lỗi: Dữ liệu Excel bị trùng lặp] --> EndErr
    CheckDupExcel -- Hợp lệ --> HashExcelPass[Mã hóa Mật khẩu mặc định bằng Bcrypt]
    HashExcelPass --> InsertExcel[(Lưu danh sách vào bảng employees & users)]
    InsertExcel --> CommitDB[(Commit Transaction: Xác nhận lưu DB)] --> CheckLeader

    CheckLeader{Chức vụ thuộc nhóm<br/>Trưởng phòng?}
    CheckLeader -- Đúng --> ElevateRole[Nâng users.role = manager & Gán manager_id cho Phòng ban]
    CheckLeader -- Không --> KeepRole[Giữ vai trò mặc định employee]
    
    ElevateRole --> Success[Thông báo Thêm / Import Nhân viên thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepRole --> Success
```

#### Sơ đồ Hoạt động 2b: Chỉnh sửa Hồ sơ Nhân viên (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Sửa Nhân viên]) --> Input[Admin/HR Nhập thông tin chỉnh sửa Hồ sơ]
    Input --> CheckExist{Nhân viên có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy nhân viên] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> CheckDupEmail{Email mới có trùng<br/>với nhân viên khác?}
    
    CheckDupEmail -- Trùng email --> ErrEmail[Báo lỗi: Email đã được sử dụng bởi người khác] --> EndErr
    CheckDupEmail -- Không trùng --> UpdateEmp[(Cập nhật thông tin vào bảng employees)]
    
    UpdateEmp --> CheckPosChange{Có sự thay đổi<br/>Chức vụ Trưởng phòng?}
    CheckPosChange -- Có thay đổi --> SyncRole[Đồng bộ lại vai trò users.role & Cập nhật manager_id phòng ban]
    CheckPosChange -- Không thay đổi --> KeepState[Bỏ qua bước đồng bộ role]
    
    SyncRole --> Success[Thông báo Cập nhật hồ sơ Nhân viên thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepState --> Success
```

#### Sơ đồ Hoạt động 2c: Khóa Hồ sơ / Cho thôi việc (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Khóa Hồ sơ]) --> ClickLock[Admin/HR Bấm nút Khóa / Cho thôi việc & Xác nhận]
    ClickLock --> CheckExist{Nhân viên có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy nhân viên] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> UpdateStatus[(Cập nhật employees.status = 'Nghi viec')]
    
    UpdateStatus --> CheckIsManager{Nhân viên có đang làm<br/>Trưởng phòng không?}
    CheckIsManager -- Đang làm Trưởng phòng --> UnsetManager[Reset manager_id = NULL & Hạ users.role = 'employee']
    CheckIsManager -- Không phải Trưởng phòng --> DeactUser[Vô hiệu hóa tài khoản users.is_active = 0]
    
    UnsetManager --> DeactUser
    DeactUser --> Success[Thông báo Khóa hồ sơ & Cho thôi việc thành công] --> EndSuccess([Kết thúc - Thành công])
```

---

### 3. QUẢN LÝ HỢP ĐỒNG LAO ĐỘNG & YÊU CẦU HỒ SƠ

#### Sơ đồ Hoạt động 3a-1: Tạo mới Hợp đồng Lao động (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Tạo Hợp đồng]) --> Input[Admin/HR Chọn Nhân viên & Nhập thông tin Hợp đồng mới]
    Input --> CheckEmpStatus{Nhân viên có đang<br/>ở trạng thái Nghi viec?}
    
    CheckEmpStatus -- Đã nghỉ việc --> ErrStatus[Báo lỗi: Nhân viên đã nghỉ việc, không thể tạo Hợp đồng] --> EndErr([Kết thúc - Lỗi])
    CheckEmpStatus -- Đang làm việc --> CheckActiveContract{Nhân viên đã có Hợp đồng<br/>đang Hiệu lực?}
    
    CheckActiveContract -- Đã có HĐ hiệu lực --> ErrActive[Báo lỗi: Nhân viên đang có Hợp đồng còn hiệu lực] --> EndErr
    CheckActiveContract -- Chưa có HĐ active --> InsertContract[(Lưu Hợp đồng mới vào bảng contracts với status = Hieu luc)]
    
    InsertContract --> Success[Thông báo Tạo mới Hợp đồng lao động thành công] --> EndSuccess([Kết thúc - Thành công])
```

#### Sơ đồ Hoạt động 3a-2: Gia hạn Hợp đồng & Xem Cảnh báo 30 ngày (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Gia hạn Hợp đồng]) --> AlertList[Quét & Hiển thị danh sách Hợp đồng sắp hết hạn trong 30 ngày]
    AlertList --> SelectContract[Admin/HR Chọn Hợp đồng cần gia hạn & Nhập Ngày kết thúc mới]
    
    SelectContract --> CheckEndDate{Ngày kết thúc mới<br/>có hợp lệ (> Ngày bắt đầu)?}
    CheckEndDate -- Không hợp lệ --> ErrDate[Báo lỗi: Ngày kết thúc mới phải lớn hơn Ngày bắt đầu] --> EndErr([Kết thúc - Lỗi])
    
    CheckEndDate -- Hợp lệ --> UpdateOldContract[(Cập nhật trạng thái Hợp đồng cũ status = Da gia han)]
    UpdateOldContract --> CreateNewContract[(Tạo Hợp đồng gia hạn mới với Thời hạn mới vào DB)]
    
    CreateNewContract --> Success[Thông báo Gia hạn Hợp đồng lao động thành công] --> EndSuccess([Kết thúc - Thành công])
```

#### Sơ đồ Hoạt động 3a-3: Chấm dứt Hợp đồng Lao động (Admin / HR)
```text
flowchart TD
    Start([Bắt đầu Chấm dứt Hợp đồng]) --> SelectContract[Admin/HR Chọn Hợp đồng & Nhập Ngày chấm dứt, Lý do]
    SelectContract --> CheckExist{Hợp đồng có<br/>tồn tại không?}
    
    CheckExist -- Không thấy --> ErrExist[Báo lỗi: Không tìm thấy Hợp đồng] --> EndErr([Kết thúc - Lỗi])
    CheckExist -- Có tồn tại --> UpdateContract[(Cập nhật status = Da cham dut & Cập nhật Ngày kết thúc)]
    
    UpdateContract --> CheckDeactEmp{Có lựa chọn Cho thôi việc<br/>nhân viên này luôn không?}
    CheckDeactEmp -- Có cho nghỉ --> DeactEmp[(Cập nhật employees.status = 'Nghi viec' & Vô hiệu hóa Tài khoản)]
    CheckDeactEmp -- Không cho nghỉ --> KeepEmp[Giữ trạng thái Nhân viên bình thường]
    
    DeactEmp --> Success[Thông báo Chấm dứt Hợp đồng lao động thành công] --> EndSuccess([Kết thúc - Thành công])
    KeepEmp --> Success
```

#### Sơ đồ Hoạt động 3b: Đề xuất & Duyệt Yêu cầu Thay đổi SĐT / Địa chỉ (Nhân viên & HR)
```text
flowchart TD
    Start([Bắt đầu Yêu cầu Hồ sơ]) --> RoleAction{Tác vụ của Người dùng?}
    
    RoleAction -- 1. Nhân viên Đề xuất --> InputNewInfo[Nhập SĐT mới hoặc Địa chỉ mới & Bấm Gửi]
    InputNewInfo --> CheckSame{Thông tin mới có<br/>trùng với dữ liệu cũ?}
    CheckSame -- Trùng cũ --> ErrSame[Báo lỗi: Thông tin mới không có thay đổi] --> EndErr([Kết thúc - Lỗi])
    CheckSame -- Khác cũ --> SaveReq[(Lưu yêu cầu vào profile_change_requests với status = Cho duyet)] --> EndSub([Gửi Đề xuất Thành công])
    
    RoleAction -- 2. HR Phê duyệt --> SelectReq[HR Chọn Yêu cầu đổi SĐT/Địa chỉ cần xử lý]
    SelectReq --> CheckEmpActive{Nhân viên còn đang<br/>làm việc không?}
    CheckEmpActive -- Đã nghỉ việc --> ErrDeact[Báo lỗi: Nhân viên đã nghỉ việc, không thể duyệt] --> EndErr
    CheckEmpActive -- Đang làm việc --> Decision{HR Quyết định?}
    
    Decision -- TỪ CHỐI --> RejectReq[(Cập nhật status = Tu choi & Lưu lý do)] --> EndReject([Từ chối Yêu cầu])
    Decision -- PHÊ DUYỆT --> ApproveReq[(Cập nhật status = Da duyet)]
    ApproveReq --> UpdateProfile[(Cập nhật trực tiếp SĐT/Địa chỉ mới vào bảng employees)] --> EndApprove([Phê duyệt & Cập nhật Hồ sơ Thành công])
```

---

### 4. QUẢN LÝ CHẤM CÔNG THÔNG MINH (QR TOTP + GPS)

#### Sơ đồ Hoạt động 4: Chấm công Check-in / Check-out (Nhân viên / Quản lý)
```text
flowchart TD
    Start([Bắt đầu Chấm công]) --> Scan[Quét mã QR TOTP 30s + Bật định vị GPS]
    Scan --> CheckQR{Mã QR có hợp lệ<br/>trong vòng 30 giây?}
    
    CheckQR -- Hết hạn/Sai --> ErrQR[Báo lỗi: Mã QR không hợp lệ] --> EndErr([Kết thúc - Lỗi])
    CheckQR -- Hợp lệ --> CheckGPS{Tọa độ GPS nằm trong<br/>bán kính công ty?}
    
    CheckGPS -- Ngoài phạm vi --> ErrGPS[Báo lỗi: Đang ở ngoài phạm vi công ty] --> EndErr
    CheckGPS -- Trong công ty --> CheckLeave{Hôm nay đã được<br/>duyệt nghỉ phép chưa?}
    
    CheckLeave -- Đã nghỉ --> ErrLeave[Báo lỗi: Hôm nay bạn đã được duyệt nghỉ phép] --> EndErr
    CheckLeave -- Chưa nghỉ --> CheckDup{Hôm nay đã Check-in<br/>trước đó chưa?}
    
    CheckDup -- Đã Check-in --> ActionCheck{Tác vụ hiện tại là?}
    ActionCheck -- Check-in tiếp --> ErrDup[Báo lỗi: Hôm nay bạn đã Check-in rồi] --> EndErr
    ActionCheck -- Check-out --> CalcWork[Tính tổng số phút làm việc & Kiểm tra về sớm < 17:00] --> UpdateOut[(Cập nhật giờ Check-out vào DB)] --> EndOut([Check-out Thành công])
    
    CheckDup -- Chưa Check-in --> DetermineTime[Xác định trạng thái: Đúng giờ / Đi trễ]
    DetermineTime --> SaveIn[(Lưu giờ Check-in mới vào DB)] --> EndIn([Check-in Thành công])
```

---

### 5. QUẢN LÝ ĐƠN XIN NGHỈ PHÉP & PHÊ DUYỆT 3 TẦNG

#### Sơ đồ Hoạt động 5: Nộp & Duyệt Đơn xin Nghỉ phép (Nhân viên / Quản lý / HR / Admin)
```text
flowchart TD
    Start([Bắt đầu Nghiệp vụ Đơn phép]) --> RoleAction{Tác vụ của Người dùng?}
    
    RoleAction -- Nộp đơn nghỉ phép --> FillForm[Nhập Loại đơn, Ngày bắt đầu, Ngày kết thúc, Lý do]
    FillForm --> CheckOverlap{Khoảng ngày xin nghỉ<br/>có bị trùng đơn cũ?}
    CheckOverlap -- Bị trùng --> ErrOverlap[Báo lỗi: Khoảng ngày nghỉ bị đè trùng lên đơn cũ] --> EndErr([Kết thúc - Lỗi])
    CheckOverlap -- Không trùng --> CountDays[Tự động đếm số ngày làm việc thực tế: Trừ Chủ nhật]
    CountDays --> SaveRequest[(Lưu đơn nghỉ phép mới với status = Cho duyet)] --> EndSubmit([Gửi đơn Thành công])
    
    RoleAction -- Phê duyệt Đơn --> SelectRequest[Chọn Đơn xin nghỉ phép cần xử lý]
    SelectRequest --> CheckSelf{Người duyệt có phải<br/>chính chủ đơn không?}
    CheckSelf -- Đúng --> ErrSelf[Báo lỗi HTTP 403: Không thể tự duyệt đơn của chính mình] --> EndErr
    CheckSelf -- Không --> Decision{Quyết định duyệt?}
    
    Decision -- PHÊ DUYỆT --> UpdateApprove[Cập nhật status = Da duyet] --> AddAbsent[(Tự động tạo các ngày Vắng mặt trên bảng attendances)] --> EndApprove([Phê duyệt Thành công])
    Decision -- TỪ CHỐI --> InputReason[Nhập lý do từ chối] --> UpdateReject[Cập nhật status = Tu choi] --> CleanAbsent[(Tự động xóa lịch Vắng mặt trên bảng attendances)] --> EndReject([Từ chối Thành công])
```

---

### 6. BÁO CÁO, THỐNG KÊ & TRÍCH XUẤT DỮ LIỆU

#### Sơ đồ Hoạt động 6: Xem Báo cáo Thống kê & Trích xuất File Excel (.xlsx) (Admin / HR / Quản lý)
```text
flowchart TD
    Start([Bắt đầu Xem Báo cáo]) --> SelectFilter[Chọn Bộ lọc: Tháng, Năm & Phòng ban]
    SelectFilter --> InitTZ[Khởi tạo Tháng/Năm mặc định theo Múi giờ Việt Nam Asia/Ho_Chi_Minh]
    
    InitTZ --> CheckRole{Vai trò Người dùng?}
    CheckRole -- Quản lý (Trưởng phòng) --> QueryDept[(Truy vấn số liệu thống kê riêng cho Phòng ban phụ trách)]
    CheckRole -- Admin / HR --> QueryCompany[(Truy vấn số liệu thống kê tổng hợp toàn bộ Công ty)]
    
    QueryDept --> RenderUI[Hiển thị Bảng công, Tỷ lệ nghỉ phép & Biểu đồ thống kê]
    QueryCompany --> RenderUI
    
    RenderUI --> ClickExport{Bấm nút "Xuất file Excel"?}
    ClickExport -- Không xuất --> EndView([Xem báo cáo xong])
    ClickExport -- Có xuất Excel --> GenerateExcel[Trích xuất dữ liệu thành file định dạng .xlsx]
    GenerateExcel --> DownloadFile[Tải file Excel báo cáo về máy tính thành công] --> EndExport([Xuất Excel Thành công])
```










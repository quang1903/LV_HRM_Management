# Nhật ký công việc ngày 21/7/2026

## 🏆 NHỮNG GÌ ĐÃ SỬA HÔM NAY

### 1. Ứng dụng phân quyền trang `/users` cho Manager (Sidebar & Next.js Router)
* **Frontend ([sidebar.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/sidebar.tsx#L23))**: 
  * Chuyển icon của mục "Người dùng" từ `Shield` sang `Users`.
  * Thêm cấu hình `roles: ["admin", "hr"]` để loại bỏ quyền hiển thị của cấp `manager` trên menu.
  * Cấu trúc lại logic lọc menu `visibleMenu = mainMenu.filter(...)` để ẩn các mục dựa trên thuộc tính `roles` và vai trò `user.role` hiện tại.
* **Đồng bộ định tuyến**:
  * **[middleware.ts](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/middleware.ts#L13)**: Xóa `"manager"` khỏi quyền truy cập Router Next.js cho đường dẫn `/users`.
  * **[AuthContext.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/context/AuthContext.tsx#L39)**: Xóa `"manager"` khỏi danh sách `PAGE_PERMISSIONS` đối với trang `/users` ở Client-side.

### 2. Vá lỗ hổng bảo mật phân quyền API Báo cáo
* **Backend ([report.routes.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/routes/report.routes.js#L7-L10))**:
  * Import thêm `roleMiddleware` từ auth middleware.
  * Gắn `roleMiddleware("admin", "hr", "manager")` cho cả 4 API lấy dữ liệu báo cáo nhạy cảm (`/attendance`, `/department`, `/leave`, `/contract`). Nhân viên thường (`employee`) cố gọi API trực tiếp qua Postman/DevTools giờ sẽ bị chặn và trả về mã lỗi `403 Forbidden`.

### 3. Khắc phục Bug mất đồng bộ Trưởng phòng khi thuyên chuyển nội bộ
* **Backend ([employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L169-L174))**:
  * Cập nhật logic trong hàm `updateEmployee`: Khi nâng chức vụ một nhân viên làm Trưởng phòng mới (`empDeptId`), hệ thống tự động SET `manager_id = NULL` ở các phòng ban cũ mà nhân viên này từng làm trưởng phòng trước đó. Sau đó mới gán làm `manager_id` cho phòng ban mới, loại bỏ lỗi "nhân viên làm quản lý đồng thời nhiều phòng ban".

### 4. Khắc phục rò rỉ thông tin mật tại Settings API
* **Backend ([settings.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/settings.controller.js#L5-L7))**:
  * Thay thế truy vấn `SELECT * FROM settings WHERE id = 1` trong hàm `getSettings` bằng câu lệnh chỉ định rõ các trường an toàn: `SELECT company_lat, company_lng, max_distance, device_lock_enabled`.
  * Điều này giúp bảo vệ hoàn toàn `scan_password` và `scan_token` của máy quét khỏi việc bị trả về phía Client-side của nhân viên thường.

### 5. Tăng kích thước camera & vùng quét QR trên máy chấm công
* **Frontend ([scan/page.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/app/scan/page.tsx))**:
  * **Dòng 208**: Tăng chiều rộng bao ngoài của giao diện máy quét từ `max-w-md` lên `max-w-2xl`.
  * **Dòng 221**: Thay thế thuộc tính `aspect-square` thành `w-full relative` kèm chiều cao cố định `height: "600px"` để camera hiển thị cao rộng hơn.
  * **Dòng 71**: Tăng vùng bắt mã quét `qrbox` của thư viện `Html5Qrcode` từ kích thước tĩnh `250` lên `{ width: 500, height: 500 }`.

### 6. Thêm thời gian chờ (cooldown) giữa Check-in và Check-out
* **Backend ([attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js#L208-L211))**:
  * Cập nhật hàm `checkOut`: Tính khoảng cách thời gian `diffMinutes` giữa thời điểm check-in hiện có với thời điểm quét check-out.
  * Nếu khoảng cách này ngắn hơn 3 giây (`diffMinutes < 0.05`), hệ thống sẽ trả về lỗi `400` kèm thông báo: *"Bạn vừa check-in, vui lòng chờ ít nhất 3 giây trước khi check-out"*, tránh lỗi quét trùng mã liên tiếp.

### 7. Sửa lỗi đếm ngược và tự động làm mới mã QR chấm công của Nhân viên
* **Frontend ([my-qr-code.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/my-qr-code.tsx))**:
  * **Đồng bộ hóa bộ đếm**: Chuyển logic đếm ngược và tự động fetch mã vào một `useEffect` duy nhất phụ thuộc vào `[qrImage, expired, fetchCount]`.
  * **Hủy interval triệt để**: Thực hiện gọi `clearInterval(intervalRef.current)` ngay lập tức khi hàm `fetchQR()` vừa bắt đầu chạy để ngắt hẳn bộ đếm cũ, tránh tình trạng đếm ngược chạy ngầm trong lúc API đang tải dữ liệu.
  * **Tạo state fetchCount**: Thêm state `fetchCount` đóng vai trò là một nonce thay đổi sau mỗi lần fetch thành công, ép `useEffect` luôn dọn dẹp và khởi chạy lại bộ đếm từ thời gian thực tế `expires_in` của Server dù ảnh QR trả về trùng lặp (trùng OTP cũ do cùng nằm trong 1 khung 30s).
  * **Chặn bấm đúp (Double-click Guard)**: Thêm điều kiện `if (refreshing || loading) return` vào `handleRefresh` và ẩn nút "Tạo mã mới" khi `loading` để ngăn hoàn toàn việc click liên tiếp gây xung đột chạy song song nhiều bộ đếm.

### 8. Cập nhật thông báo trạng thái check-in/check-out của Máy quét
* **Frontend ([scan/page.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/app/scan/page.tsx))**:
  * Thay đổi thông báo thành công check-in/check-out tiếng Việt tự nhiên hơn: *"Đã vào lúc [Giờ]"* và *"Đã ra lúc [Giờ]"*.
  * Thêm logic bắt lỗi trùng lặp khi quét lần thứ 3 (khi nhân viên đã hoàn thành cả check-in lẫn check-out trong ngày) để hiển thị thông báo *"Đã điểm danh đủ hôm nay"*.
  * Tạm dừng camera quét 3 giây sau mỗi lần quét thành công (Check-in, Check-out, hoặc báo trùng điểm danh) bằng cách gọi `scannerRef.current?.pause(true)` và tự động `resume()` sau 3s, giúp người dùng có đủ thời gian cất điện thoại đi và tránh hiện tượng quét liên tiếp 2 lần cùng lúc.
### 9. Cải thiện hiển thị giờ làm việc trên bảng Chấm công
* **Frontend ([attendance-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/attendance-table.tsx))**:
  * Thay thế kiểm tra `att.work_minutes` bằng `att.check_out` để tránh bẫy falsy của số `0` trong JS.
  * Hiển thị chữ `"< 1 phút"` khi nhân viên đã check-out nhưng số phút làm việc bằng 0 (quét test nhanh dưới 1 phút), giúp tăng tính trực quan khi demo chấm bài với giáo viên.
### 10. Sắp xếp danh sách Chấm công theo giờ check-in mới nhất
* **Backend ([attendance.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/attendance.controller.js))**:
  * Thay đổi cấu trúc sắp xếp của danh sách chấm công từ `e.full_name ASC` sang `a.check_in DESC`.
  * Giúp cho các lượt chấm công mới nhất trong ngày luôn được đẩy lên đầu bảng ngay khi quét QR thành công, giúp việc giám sát chấm công trực quan hơn.

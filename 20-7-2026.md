# Nhật ký công việc ngày 20/7/2026

## 🏆 NHỮNG GÌ ĐÃ SỬA HÔM NAY

### 1. Sửa lỗi đỏ (TypeScript Error) trong [employee-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/employee-table.tsx)
* Thêm thuộc tính `id_card: string | null` vào định nghĩa kiểu dữ liệu `Employee`.
* Thêm `id_card: ""` vào state `newEmployee` ban đầu và trong logic reset form `setNewEmployee` để tránh lỗi lệch kiểu.
* Loại bỏ tất cả các từ khóa ép kiểu `as any` không an toàn quanh các thuộc tính dùng `id_card`.

### 2. Tích hợp Component CalendarPicker tự chế
* Viết component `CalendarPicker` bằng tiếng Việt hỗ trợ chọn ngày, tháng, năm trực quan, tránh lỗi của ô nhập date mặc định trên một số trình duyệt.
* Tích hợp component này thay cho ô ngày sinh ở cả modal **Thêm** và **Sửa** nhân viên.
* Thay đổi css của popup calendar thành `bottom-full mb-1` để popup mở hướng lên trên thay vì bị khuất dưới modal.
* Khắc phục các lỗi kiểu dữ liệu nghiêm ngặt trong `CalendarPicker`:
  * Dùng `globalThis.MouseEvent` cho sự kiện click-out để tránh xung đột kiểu sự kiện React.
  * Sử dụng toán tử spread `[ ... ]` gộp mảng `cells` thay cho `.concat()` để tránh lỗi lệch kiểu dữ liệu của TypeScript.
  * Định nghĩa kiểu `: any` cho các tham số ẩn `_` và `day` ở vòng lặp `.map` và `Array.from` để đáp ứng cấu hình `noImplicitAny: true`.

### 3. Tùy biến CSS lịch trong [globals.css](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/app/globals.css)
* Thêm override style cho `input[type="date"]` để icon lịch và màu sắc hiển thị đẹp, hòa hợp với giao diện hệ thống khi dùng input date tiêu chuẩn.

### 4. Sửa lỗi lọc trưởng phòng theo phòng ban trong [department-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/department-table.tsx)
* Thêm trường `department_id: number | null` vào kiểu dữ liệu `Employee` trong department-table.
* **Modal Chỉnh sửa**: Cấu hình bộ lọc chỉ hiển thị nhân viên thuộc đúng phòng ban đang được sửa (`e.department_id === editDept.id`), ngăn việc gán trưởng phòng thuộc phòng ban khác.
* **Modal Thêm**: Chuyển text thành `-- Chọn trưởng phòng (có thể gán sau) --` và hiển thị thêm tên phòng ban hiện tại của nhân viên để tránh gán nhầm.

### 5. Loại bỏ phòng ban "Nhan su" khỏi cảnh báo thiếu Trưởng phòng trong [page.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/app/page.tsx)
* Thêm điều kiện lọc `d.name !== "Nhan su"` để ngăn cảnh báo thiếu Trưởng phòng đối với phòng Nhân sự.

### 6. Đồng bộ hóa Chức vụ chuyên môn khi gán/bỏ Trưởng phòng trong [department.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/department.controller.js)
* Tự động gán chức vụ "Trưởng phòng..." cho nhân viên mới được bổ nhiệm làm Trưởng phòng ở trang Phòng ban.
* Tự động hoàn trả về chức vụ bình thường của phòng ban cho người cũ bị tháo chức danh Trưởng phòng.

### 7. Đồng bộ hóa ngược từ Chức vụ Nhân sự sang Phòng ban & Quyền hạn trong [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js)
* **Hàm `importEmployees`**: Tự động gán `position_id` của Trưởng phòng cho người được import có vai trò `manager`.
* **Hàm `updateEmployee`**:
  * Khi nâng chức vụ lên chứa chữ `"Trưởng phòng"`: Tự động chuyển quyền `users.role = 'manager'` và gán làm `manager_id` của phòng ban đó.
  * Khi hạ chức vụ xuống nhân viên thường (không chứa chữ `"Trưởng phòng"`): Tự động hạ quyền tài khoản về `'employee'` và xóa tư cách Trưởng phòng (`manager_id = NULL`) khỏi phòng ban.

### 8. Tích hợp tính năng Reset thiết bị (Device Lock) toàn hệ thống
* **Backend**: 
  * Viết API `resetDeviceAll` trong [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js) và đăng ký route `/employees/reset-device-all` trong [employee.routes.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/routes/employee.routes.js) cho phép đặt lại thiết bị cho toàn bộ người dùng (trừ admin).
* **Frontend**:
  * Thêm phương thức gọi API `resetDeviceAll` trong [employee.ts](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/services/employee.ts).
  * Cập nhật [user-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/user-table.tsx): Thay thế hộp thoại prompt thô sơ bằng **Modal Reset Thiết Bị** chuyên nghiệp, cho phép gộp cả 2 tính năng: Reset toàn hệ thống hoặc Reset riêng biệt theo từng phòng ban tự chọn. Chỉ hiển thị nút **Reset thiết bị** và **Tạo tài khoản** cho người dùng có vai trò `admin` hoặc `hr`.

### 9. Bổ sung quyền truy cập trang `/positions`, `/settings`, `/profile` và `/profile-requests` cho HR
* Cập nhật `PAGE_PERMISSIONS` trong [AuthContext.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/context/AuthContext.tsx) để mở quyền truy cập trang quản lý Chức vụ (`/positions`) và Cài đặt (`/settings`) cho vai trò `hr` (Nhân sự) và `admin`.
* Cập nhật `ROLE_PERMISSIONS` trong [middleware.ts](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/middleware.ts) để thêm đồng bộ các đường dẫn `/settings`, `/profile`, và `/profile-requests` cho vai trò `hr` và `admin`.

### 10. Tích hợp SQL Transaction trong hàm `createEmployee`
* Sử dụng `pool.getConnection()`, `beginTransaction()`, `commit()` và `rollback()` khi tạo mới Nhân sự nhằm đảm bảo nếu quá trình tạo tài khoản `users` bị lỗi, hồ sơ tương ứng trong bảng `employees` sẽ được thu hồi hoàn toàn, tránh dữ liệu rác mồ côi.

### 11. Đồng bộ hóa tên Tiếng Việt có dấu của Phòng ban và Chức vụ
* Thay đổi các chuỗi lọc hardcode không dấu (như `Truong phong%`, `truong phong`, `Ky thuat`) thành tiếng Việt có dấu chuẩn (`Trưởng phòng%`, `trưởng phòng`, `Kỹ thuật`) trong:
  * [department.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/department.controller.js)
  * [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js)
  * [employee-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/employee-table.tsx)
* Điều này giúp logic tự động bổ nhiệm, hạ chức danh Trưởng phòng và tải file mẫu hoạt động khớp 100% với tên có dấu mới được cập nhật trong database.

### 12. Tích hợp Lọc Trạng thái và Phân trang (Pagination)
* **Chấm công ([attendance-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/attendance-table.tsx))**: Tích hợp thêm bộ chọn lọc theo Trạng thái (Đúng giờ, Đi trễ, Về sớm, Vắng mặt) và phân trang (mỗi trang 20 dòng) kèm bộ nút chuyển trang ở cuối bảng.
* **Hợp đồng ([contract-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/contract-table.tsx))**: Tích hợp phân trang (20 dòng/trang) và bộ nút chuyển trang ở cuối bảng.
* **Nghỉ phép ([leave-table.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/leave-table.tsx))**: Tích hợp bộ chọn lọc theo Trạng thái nghỉ phép (Chờ duyệt, Đã duyệt, Từ chối) trước nút Gửi đơn.

### 13. Tự động đồng bộ chấm công Vắng mặt khi Duyệt nghỉ phép
* Cập nhật hàm `approveLeave` tại [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js): Khi đơn nghỉ phép được duyệt, backend tự động quét qua tất cả các ngày trong kỳ nghỉ (trừ ngày Chủ nhật). Nếu chưa có bản ghi chấm công cho ngày đó, tự động thêm mới bản ghi có trạng thái `'Vang mat'`. Nếu đã có, cập nhật trạng thái về `'Vang mat'` và xóa trắng giờ vào/ra.

### 14. Tích hợp 2 biểu đồ trực quan (Chart.js) trong trang Báo cáo
* **Frontend ([reports-panel.tsx](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/components/hrm/reports-panel.tsx))**:
  * Tích hợp thư viện `chart.js` để hiển thị biểu đồ.
  * Thêm biểu đồ cột **Chấm công theo ngày** (Bar Chart) mô tả số lượng nhân viên đi làm thực tế theo từng ngày trong tháng đã chọn.
  * Thêm biểu đồ tròn **Tỷ lệ chấm công** (Doughnut Chart) biểu diễn trực quan cơ cấu tỷ lệ Đúng giờ, Đi trễ, Về sớm, Vắng mặt trong tháng.

---

## ⏳ NHỮNG GÌ CHƯA SỬA (Các nhiệm vụ tồn đọng và cần làm tiếp)

### 1. Lỗi chấm công ca đêm (Hạn chế cốt lõi)
* Cần cập nhật `attendance.controller.js` ở Backend: Bỏ lọc cứng `work_date = today` trong hàm `checkOut` và thay bằng điều kiện lọc động trong vòng 24 giờ qua: `check_in >= DATE_SUB(NOW(), INTERVAL 24 HOUR)`.

### 2. Lỗ hổng bảo mật hệ thống
* **GPS Spoofing (API Chấm công)**: Backend đang tin tưởng tọa độ client gửi lên trực tiếp. Đề xuất: Lưu tọa độ cố định gắn với `Terminal Token` trong DB.
* **Device Lock Takeover**: Tài khoản mới tạo chưa đăng nhập có thể bị người khác đăng nhập trước để khóa thiết bị. Đề xuất: Chỉ cho Admin kích hoạt hoặc gửi Activation Code dùng 1 lần về Email.

### 3. Các tính năng bàn giao còn lại
* **Xuất báo cáo ra Excel**: Tích hợp logic xuất Excel hoàn chỉnh ở trang Báo cáo.
* **Trang cá nhân (Profile)**: Cho phép nhân viên tự cập nhật thông tin cá nhân (SĐT, địa chỉ, ảnh đại diện) và duyệt yêu cầu.
* **Đổi mật khẩu**: Tích hợp chức năng đổi mật khẩu cá nhân (xác thực bcrypt mật khẩu cũ) vào trang Profile.

### 5. Cấu hình phân quyền bổ sung trong middleware.ts
* Cần cập nhật `ROLE_PERMISSIONS` trong [middleware.ts](file:///c:/Users/quang/Desktop/LV_HRM_Management/frontend/middleware.ts) để bổ sung các đường dẫn `/settings` và `/profile-requests` cho vai trò `hr` và `admin`.


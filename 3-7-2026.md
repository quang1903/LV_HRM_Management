# Ghi chú ngày 3/7/2026

## Các file đã thay đổi
1. `frontend/app/page.tsx`
2. `frontend/components/hrm/user-table.tsx`
3. `frontend/components/hrm/topbar.tsx`

## Nội dung thay đổi
1. **Giao diện Cảnh báo Dashboard (page.tsx):**
   - **Cập nhật:** Đã loại bỏ banner tĩnh và thay thế bằng một **Popup Modal** nổi bật hiện ra ngay giữa màn hình khi người dùng đăng nhập vào Dashboard.
   - Modal hiển thị thông báo với giao diện header màu đỏ cực kỳ bắt mắt để thu hút sự chú ý.
   - Vẫn giữ nguyên logic cũ: Chỉ hiển thị cho Admin và HR, fetch dữ liệu phòng ban thiếu Trưởng phòng (`!manager_id`), và có thẻ `<Link>` bao trùm toàn bộ Modal để click vào là chuyển hướng ngay sang trang quản lý Phòng ban.

2. **Cải tiến Chuông thông báo (topbar.tsx):**
   - Cập nhật Component `Topbar` để nhận prop `missingManagers` truyền từ `page.tsx`.
   - **Biểu tượng:** Thêm một bong bóng nhỏ màu đỏ phía trên chuông (badge) hiển thị tổng số lượng cảnh báo hiện tại.
   - **Dropdown:** Khi click vào chuông, một hộp thoại xổ xuống (Dropdown) sẽ xuất hiện, liệt kê chi tiết cảnh báo (ví dụ: "3 phòng ban chưa có Trưởng phòng").
   - Click ra ngoài vùng chọn (click outside) thì hộp thoại sẽ tự động đóng lại (sử dụng `useRef` và sự kiện `mousedown`).

3. **Cập nhật nút lọc trong danh sách Tài khoản (user-table.tsx):**
   - **Giao diện:** Đổi nhãn của nút lọc từ *"Chỉ trưởng phòng"* thành tên chính xác hơn là **"Quản lý & HR"**.
   - **Logic:** Thay đổi logic lọc `matchManager` sang việc quét dựa trên quyền hạn: `u.role === "manager" || u.role === "hr"`. Nhờ vậy, nút này sẽ gom chung hiển thị cả nhân sự HR và các cấp Quản lý vào một chỗ.

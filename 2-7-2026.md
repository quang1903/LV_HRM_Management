# Ghi chú ngày 2/7/2026

## Các file đã thay đổi
1. `frontend/app/globals.css`
2. `frontend/components/hrm/sidebar.tsx`
3. `frontend/components/hrm/topbar.tsx`
4. `frontend/components/hrm/stat-cards.tsx`
5. `frontend/app/login/page.tsx`

## Nội dung thay đổi (Cập nhật giao diện Dashboard)
1. **Cập nhật theme (CSS Variables):** 
   - Thay đổi các biến `--sidebar` trong file `globals.css` để chuyển đổi giao diện của Sidebar sang tông màu tối (dark theme).

2. **Cấu trúc Sidebar mới:**
   - Viết lại toàn bộ component `Sidebar`.
   - Áp dụng các tông màu tối mới.
   - Hiệu ứng hover và trạng thái active mượt mà hơn.
   - Bố cục gọn gàng, có thêm phần hiển thị thông tin User (tên, chức vụ) ở cuối Sidebar.

3. **Cấu trúc Topbar mới:**
   - Viết lại toàn bộ component `Topbar`.
   - Thêm Breadcrumb (đường dẫn hiển thị tên trang hiện tại).
   - Tối ưu hóa layout và responsive cho các nút công cụ (Thông báo, Thêm nhân viên).

4. **Stat Cards mới (Thẻ thống kê):**
   - Cập nhật giao diện của các thẻ hiển thị số liệu tổng quan trên Dashboard.
   - Thêm các biểu tượng xu hướng (mũi tên hướng lên/xuống) và style riêng biệt (màu sắc, icon) cho từng loại thẻ (Tổng nhân viên, Chấm công, Đơn nghỉ phép, Hợp đồng).

5. **Fix lỗi thanh scroll và layout Sidebar:**
   - Thêm CSS tùy chỉnh vào `globals.css` (class `.sidebar-nav`) để ẩn thanh cuộn thô kệch của Windows giúp Sidebar đẹp hơn nhưng vẫn cuộn mượt mà.
   - Sửa cấu trúc Flexbox (`min-h-0`, `flex-1`) để giải quyết tình trạng vỡ cấu trúc và tràn layout (layout overflow) ở Sidebar.

6. **Làm mới giao diện Đăng nhập (Login Page):**
   - Thay đổi toàn bộ layout của `login/page.tsx` thành dạng 2 cột hiện đại và trực quan.
   - Bố trí khu vực giới thiệu thông tin dự án bên trái.
   - Tích hợp thêm tính năng chọn "Tài khoản Demo" giúp người test điền nhanh tài khoản (Quản trị viên, HR, Quản lý, Nhân viên).

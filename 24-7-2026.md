# Nhật ký công việc & Rà soát Lỗi ngày 24/7/2026

Tài liệu này lưu trữ chi tiết danh sách các **lỗi luồng hoạt động, bất đồng bộ phân quyền và rủi ro bảo mật** đã được rà soát trong hệ thống LV_HRM_Management ngày 24/7/2026.

---

## ⏳ DANH SÁCH LỖI LUỒNG HOẠT ĐỘNG & RỦI RO CHƯA LÀM / CẦN KHẮC PHỤC

### 1. Lỗi chuyển phòng ban của Nhân viên (Employee Transfer Flow)
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L160-L219) — `updateEmployee`
* **Vấn đề & Nguyên nhân:** Khi Admin chuyển một nhân viên đang là Trưởng phòng (Manager của Phòng A) sang Phòng B, nhưng không chọn lại `position_id` (hoặc chọn chức vụ nhân viên thường). Đoạn check `if (position_id)` bị bỏ qua hoặc rơi vào case không thỏa mãn, làm cho `employees.department_id` được chuyển sang Phòng B, nhưng bảng `departments` vẫn ghi nhận nhân viên đó là `manager_id` của Phòng A.
* **Giải pháp cần làm:** Khi cập nhật nhân viên, nếu `department_id` thay đổi, phải kiểm tra và tự động giải phóng `manager_id = NULL` ở phòng ban cũ nếu nhân viên đó đang quản lý.

---

### 2. Bất đồng bộ Chức vụ khi Admin hạ quyền User tại màn hình Tài khoản
* **File liên quan:** [user.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/user.controller.js#L102-L112) — `updateUser`
* **Vấn đề & Nguyên nhân:** Khi Admin hạ `role` của một User từ `manager` xuống `employee`, hệ thống tự động gỡ `manager_id = NULL` ở phòng ban nhưng **không cập nhật `position_id` trong bảng `employees`**. Nhân viên đó vẫn giữ chức vụ "Trưởng phòng...". Lần tiếp theo khi Admin vào sửa nhân viên, hệ thống sẽ tự động đẩy `role` của tài khoản lên lại `manager`.
* **Giải pháp cần làm:** Khi hạ role user từ `manager` về `employee`, cần cập nhật đồng bộ `employees.position_id` của nhân viên đó về một chức vụ nhân viên thường của phòng ban.

---

### 3. Lỗi Import Excel có Chức vụ "Trưởng phòng" nhưng Vai trò là "employee"
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L417-L449) — `importEmployees`
* **Vấn đề & Nguyên nhân:** Trong file Excel import, nếu dòng dữ liệu điền Chức vụ = "Trưởng phòng...", nhưng cột Vai trò để trống hoặc ghi "employee". Logic import chỉ gán `departments.manager_id` khi `userRole === 'manager'`. Do đó, người này được gán chức vụ Trưởng phòng nhưng tài khoản bị gán role `employee` và Phòng ban không được ghi nhận Trưởng phòng.
* **Giải pháp cần làm:** Trong vòng lặp import Excel, nếu Chức vụ có từ khóa "Trưởng phòng", cần tự động xử lý gán `manager_id` cho phòng ban và nâng `finalRole` lên `manager` (nếu phòng đó chưa có Trưởng phòng).

---

### 4. Cập nhật Chức vụ xóa nhầm `manager_id` tất cả phòng ban
* **File liên quan:** [position.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/position.controller.js#L96-L104) — `updatePosition`
* **Vấn đề & Nguyên nhân:** Khi đổi tên một chức vụ từ có chữ "Trưởng phòng" sang không có chữ "Trưởng phòng" (`!isLeader`), hệ thống thực thi `UPDATE departments SET manager_id = NULL WHERE manager_id = ?` trên **tất cả phòng ban** mà nhân viên đó quản lý, thay vì chỉ phòng ban liên kết với chức vụ đó.
* **Giải pháp cần làm:** Chỉ xóa `manager_id = NULL` đối với đúng phòng ban có `department_id` gắn liền với chức vụ đang sửa.

---

### 5. Rủi ro xóa nhầm Chấm công Vắng mặt khi Từ chối đơn nghỉ phép
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L244-L248) — `rejectLeave`
* **Vấn đề & Nguyên nhân:** Khi từ chối đơn nghỉ phép, câu lệnh `DELETE FROM attendances WHERE employee_id = ? AND work_date BETWEEN ? AND ? AND status = 'Vang mat' AND check_in IS NULL` được thực thi. Nếu nhân viên từng bị ghi nhận "Vắng mặt" thực tế/tự động trước khi nộp đơn, hành động từ chối đơn sẽ xóa luôn cả bản ghi vắng mặt hợp lệ trước đó.
* **Giải pháp cần làm:** Chỉ dọn dẹp các bản ghi vắng mặt được tạo ra do chính đơn nghỉ phép đó hoặc chỉ xóa nếu đơn đó từng ở trạng thái `Da duyet`.

---

### 6. Thiếu Database Transaction tại `updateEmployee`
* **File liên quan:** [employee.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/employee.controller.js#L160) — `updateEmployee`
* **Vấn đề & Nguyên nhân:** Các câu lệnh `UPDATE` giữa `employees`, `users`, `departments` chạy rời rạc không nằm trong Transaction (`conn.beginTransaction()`). Nếu một câu lệnh bị lỗi giữa chừng, dữ liệu giữa 3 bảng sẽ bị rách và rủi ro bất đồng bộ vĩnh viễn.
* **Giải pháp cần làm:** Bọc toàn bộ thao tác trong `conn.beginTransaction()`, `conn.commit()` và `conn.rollback()` khi có lỗi.

---

### 7. So sánh định dạng ngày ISO khi Từ chối đơn nghỉ phép
* **File liên quan:** [leave.controller.js](file:///c:/Users/quang/Desktop/LV_HRM_Management/backend/src/controllers/leave.controller.js#L247) — `rejectLeave`
* **Vấn đề & Nguyên nhân:** `start_date` và `end_date` có thể chứa chuỗi ISO (`2026-07-24T00:00:00.000Z`). Truyền trực tiếp chuỗi này vào SQL `work_date BETWEEN ? AND ?` làm so sánh chuỗi với kiểu `DATE` trong MySQL bị trượt ngày cuối.
* **Giải pháp cần làm:** Chuẩn hóa `start_date` và `end_date` về dạng `YYYY-MM-DD` trước khi truyền vào SQL query.

---

### 8. Phân trang (Pagination) & Streaming cho dữ liệu Excel lớn
* **Vấn đề & Nguyên nhân:** Hiện tại nút Xuất Excel "Tất cả" fetch toàn bộ mảng dữ liệu về frontend và render file. Khi quy mô nhân sự lên đến 10,000+ người sẽ gây tràn bộ nhớ trình duyệt hoặc treo UI.
* **Giải pháp cần làm:** Xây dựng API stream/download file Excel từ backend theo từng chunk dữ liệu.
